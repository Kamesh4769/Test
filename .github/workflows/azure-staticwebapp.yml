<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>CheckPrint — DBM Vircon</title>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
  --green:#6DBE4F;--green-dk:#3F873F;--green-lt:#e8f7e2;
  --blue:#69A9D2;--blue-dk:#0077C8;--blue-lt:#dbeafe;
  --charcoal:#3b3d43;--charcoal2:#2e3035;
  --gray10:#636569;--gray4:#BBBBBB;--gray1:#D9D8D6;
  --bg:#f0f2f5;--surface:#fff;--border:#e3e6ea;
  --text:#1a1d23;--text-sub:#636569;
  --radius:10px;
  --shadow:0 1px 12px rgba(0,0,0,.06);
  --shadow-md:0 4px 24px rgba(0,0,0,.10);
  --shadow-lg:0 8px 48px rgba(0,0,0,.14);
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html{font-size:14px;}
body{font-family:'Plus Jakarta Sans',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;line-height:1.5;-webkit-font-smoothing:antialiased;}

/* ── SIDEBAR ─────────────────────────────── */
.sidebar{position:fixed;top:0;left:0;width:232px;height:100vh;background:var(--charcoal2);display:flex;flex-direction:column;z-index:100;border-right:1px solid rgba(255,255,255,.05);}
.slogo{padding:20px 18px 16px;border-bottom:1px solid rgba(255,255,255,.07);flex-shrink:0;}
.slogo-inner{display:flex;align-items:center;gap:10px;}
.slogo-icon{width:34px;height:34px;background:var(--green);border-radius:8px;display:flex;align-items:center;justify-content:center;font-family:'Space Grotesk',sans-serif;font-weight:700;color:#fff;font-size:14px;flex-shrink:0;}
.slogo-name{font-family:'Space Grotesk',sans-serif;font-weight:700;color:#fff;font-size:15px;line-height:1.2;letter-spacing:-.01em;}
.slogo-name em{color:var(--green);font-style:normal;}
.slogo-tag{font-size:10px;color:rgba(255,255,255,.3);letter-spacing:.08em;text-transform:uppercase;margin-top:1px;}
.snav{padding:14px 8px 8px;flex:1;overflow-y:auto;}
.snav-label{font-size:10px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:rgba(255,255,255,.24);padding:0 10px 6px;margin-top:8px;display:block;}
.snav-item{display:flex;align-items:center;gap:8px;padding:8px 10px;border-radius:7px;color:rgba(255,255,255,.52);cursor:pointer;transition:all .16s;font-size:13px;font-weight:500;margin-bottom:1px;text-decoration:none;}
.snav-item:hover{background:rgba(255,255,255,.07);color:rgba(255,255,255,.88);}
.snav-item.active{background:var(--green);color:#fff;}
.snav-item svg{width:15px;height:15px;flex-shrink:0;}
.snav-item .lbl{flex:1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;}
.snav-badge{margin-left:auto;background:var(--blue-dk);color:#fff;font-size:10px;font-weight:700;padding:1px 6px;border-radius:20px;line-height:16px;flex-shrink:0;}
.snav-item.active .snav-badge{background:rgba(0,0,0,.2);}
.suser{padding:12px 16px;border-top:1px solid rgba(255,255,255,.07);display:flex;align-items:center;gap:9px;flex-shrink:0;}
.suser-av{width:30px;height:30px;border-radius:50%;background:var(--blue-dk);display:flex;align-items:center;justify-content:center;color:#fff;font-size:11px;font-weight:700;flex-shrink:0;}
.suser-name{font-size:12px;font-weight:600;color:#fff;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.suser-role{font-size:10px;color:rgba(255,255,255,.36);}

/* ── MAIN ───────────────────────────────── */
.main{margin-left:232px;min-height:100vh;display:flex;flex-direction:column;}

/* ── TOPBAR ─────────────────────────────── */
.topbar{background:var(--surface);border-bottom:1px solid var(--border);padding:0 26px;height:60px;display:flex;align-items:center;gap:12px;position:sticky;top:0;z-index:50;}
.tb-title{font-family:'Space Grotesk',sans-serif;font-size:16px;font-weight:700;letter-spacing:-.02em;}
.tb-crumb{font-size:11px;color:var(--text-sub);display:flex;align-items:center;gap:3px;margin-top:1px;}
.searchbox{flex:1;max-width:280px;display:flex;align-items:center;gap:7px;background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:6px 11px;transition:border-color .2s;}
.searchbox:focus-within{border-color:var(--green);background:#fff;}
.searchbox svg{width:13px;height:13px;color:var(--gray4);flex-shrink:0;}
.searchbox input{border:none;background:none;outline:none;font-size:12.5px;width:100%;color:var(--text);font-family:'Plus Jakarta Sans',sans-serif;}
.searchbox input::placeholder{color:var(--gray4);}
.tb-right{margin-left:auto;display:flex;align-items:center;gap:8px;}
.role-sw{display:flex;background:var(--bg);border:1px solid var(--border);border-radius:8px;padding:3px;gap:2px;}
.role-pill{padding:5px 12px;border-radius:6px;font-size:11.5px;font-weight:600;cursor:pointer;border:none;background:transparent;color:var(--text-sub);transition:all .16s;font-family:'Plus Jakarta Sans',sans-serif;white-space:nowrap;}
.role-pill.on{background:var(--charcoal);color:#fff;}
.ico-btn{width:34px;height:34px;border-radius:8px;background:var(--bg);border:1px solid var(--border);display:flex;align-items:center;justify-content:center;cursor:pointer;transition:all .16s;position:relative;flex-shrink:0;}
.ico-btn:hover{border-color:var(--green);}
.ico-btn svg{width:15px;height:15px;color:var(--text-sub);}
.notif-dot{position:absolute;top:6px;right:6px;width:6px;height:6px;background:var(--green);border-radius:50%;border:1.5px solid #fff;}

/* ── BUTTONS ────────────────────────────── */
.btn{display:inline-flex;align-items:center;gap:6px;padding:8px 15px;border-radius:8px;font-size:12.5px;font-weight:600;cursor:pointer;border:none;transition:all .16s;font-family:'Plus Jakarta Sans',sans-serif;white-space:nowrap;line-height:1.4;}
.btn svg{width:13px;height:13px;flex-shrink:0;}
.btn-primary{background:var(--green);color:#fff;} .btn-primary:hover{background:var(--green-dk);}
.btn-outline{background:transparent;color:var(--text);border:1.5px solid var(--border);} .btn-outline:hover{border-color:var(--green);color:var(--green);}
.btn-ghost{background:transparent;border:none;color:var(--text-sub);padding:6px 10px;} .btn-ghost:hover{color:var(--text);background:var(--bg);}
.btn-sm{padding:6px 11px;font-size:11.5px;}
.btn-danger{background:#dc2626;color:#fff;} .btn-danger:hover{background:#b91c1c;}
.btn-approve{background:#16a34a;color:#fff;} .btn-approve:hover{background:#15803d;}
.btn-reject{background:#dc2626;color:#fff;} .btn-reject:hover{background:#b91c1c;}
.btn-revise{background:#7c3aed;color:#fff;} .btn-revise:hover{background:#6d28d9;}

/* ── CONTENT ────────────────────────────── */
.content{flex:1;padding:22px 26px;}

/* ── STAT CARDS ─────────────────────────── */
.stats-row{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin-bottom:20px;}
.stat-card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);padding:15px 16px;position:relative;overflow:hidden;transition:box-shadow .2s,transform .2s;cursor:default;}
.stat-card:hover{box-shadow:var(--shadow-md);transform:translateY(-1px);}
.stat-card::after{content:'';position:absolute;top:0;left:0;right:0;height:3px;}
.sc-g::after{background:var(--green);} .sc-b::after{background:var(--blue-dk);} .sc-t::after{background:var(--green-dk);} .sc-a::after{background:#f59e0b;} .sc-gr::after{background:var(--gray10);}
.stat-label{font-size:10px;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--text-sub);margin-bottom:9px;}
.stat-num{font-family:'Space Grotesk',sans-serif;font-size:27px;font-weight:700;line-height:1;margin-bottom:4px;letter-spacing:-.02em;}
.stat-hint{font-size:11px;color:var(--text-sub);}
.stat-bgico{position:absolute;right:12px;bottom:8px;opacity:.06;pointer-events:none;}
.stat-bgico svg{width:38px;height:38px;}

/* ── SECTION ────────────────────────────── */
.sec-hd{display:flex;align-items:center;justify-content:space-between;margin-bottom:10px;}
.sec-title{font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;letter-spacing:-.01em;}

/* ── CARD ───────────────────────────────── */
.card{background:var(--surface);border:1px solid var(--border);border-radius:var(--radius);box-shadow:var(--shadow);overflow:hidden;}
.card-pad{padding:18px;}

/* ── TABLE ──────────────────────────────── */
table{width:100%;border-collapse:collapse;}
thead{background:#f8f9fb;}
th{padding:9px 13px;text-align:left;font-size:10.5px;font-weight:700;letter-spacing:.07em;text-transform:uppercase;color:var(--text-sub);border-bottom:1px solid var(--border);white-space:nowrap;cursor:pointer;user-select:none;}
th:first-child{padding-left:16px;}
th:hover{color:var(--text);}
th .sort-ico{display:inline-block;margin-left:3px;opacity:.4;font-size:9px;}
th.sorted .sort-ico{opacity:1;color:var(--green);}
td{padding:11px 13px;font-size:12.5px;border-bottom:1px solid #f0f2f5;vertical-align:middle;}
td:first-child{padding-left:16px;}
tbody tr{transition:background .12s;cursor:pointer;}
tbody tr:hover{background:#f5fbf2;}
tbody tr:last-child td{border-bottom:none;}
tbody tr.selected-row{background:#f0faf0 !important;box-shadow:inset 3px 0 0 var(--green);}

.cp-id{font-family:'Space Grotesk',sans-serif;font-weight:700;font-size:12.5px;color:var(--blue-dk);}
.cp-name{font-weight:600;font-size:12.5px;line-height:1.3;}
.cp-meta{font-size:11px;color:var(--text-sub);font-weight:400;margin-top:1px;}
.chip{display:flex;align-items:center;gap:6px;}
.chip-av{width:24px;height:24px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:9px;font-weight:700;color:#fff;flex-shrink:0;}
.chip-n{font-size:12px;font-weight:500;white-space:nowrap;}

/* ── BADGES ─────────────────────────────── */
.badge{display:inline-flex;align-items:center;gap:5px;padding:3px 9px;border-radius:20px;font-size:11px;font-weight:600;white-space:nowrap;line-height:1.5;}
.bd{width:5px;height:5px;border-radius:50%;flex-shrink:0;}
.b-pen{background:#fef3c7;color:#92400e;} .b-pen .bd{background:#f59e0b;}
.b-rev{background:#dbeafe;color:#1e40af;} .b-rev .bd{background:#3b82f6;}
.b-app{background:#dcfce7;color:#166534;} .b-app .bd{background:#22c55e;}
.b-rej{background:#fee2e2;color:#991b1b;} .b-rej .bd{background:#ef4444;}
.b-rvis{background:#ede9fe;color:#5b21b6;} .b-rvis .bd{background:#8b5cf6;}

.prio-cell{display:inline-flex;align-items:center;gap:5px;font-size:12px;font-weight:500;}
.pdot{width:7px;height:7px;border-radius:50%;flex-shrink:0;}
.ph{background:#ef4444;} .pm{background:#f59e0b;} .pl{background:var(--green);}

.prog{width:68px;height:4px;background:var(--gray1);border-radius:2px;overflow:hidden;}
.prog-f{height:100%;border-radius:2px;transition:width .4s;}

.arow{display:flex;align-items:center;gap:4px;}
.abtn{width:26px;height:26px;border-radius:6px;border:1px solid var(--border);background:transparent;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .16s;color:var(--text-sub);flex-shrink:0;}
.abtn:hover{border-color:var(--green);color:var(--green);background:var(--green-lt);}
.abtn svg{width:12px;height:12px;}
.abtn.aok:hover{border-color:#16a34a;color:#16a34a;background:#dcfce7;}
.abtn.ano:hover{border-color:#dc2626;color:#dc2626;background:#fee2e2;}
.abtn.astar{color:#f59e0b;border-color:#fde68a;}
.abtn.astar:hover{background:#fef9c3;}

/* ── TABS ───────────────────────────────── */
.tabs{display:flex;gap:2px;background:var(--surface);border:1px solid var(--border);border-radius:9px;padding:3px;width:fit-content;margin-bottom:14px;}
.tab{padding:6px 15px;border-radius:7px;font-size:12.5px;font-weight:500;cursor:pointer;transition:all .16s;color:var(--text-sub);border:none;background:none;font-family:'Plus Jakarta Sans',sans-serif;white-space:nowrap;}
.tab.on{background:var(--charcoal);color:#fff;}
.tab:not(.on):hover{background:var(--bg);color:var(--text);}

/* ── FILTERS ────────────────────────────── */
.frow{display:flex;align-items:center;gap:8px;margin-bottom:12px;flex-wrap:wrap;}
.fsel{padding:7px 10px;background:var(--surface);border:1px solid var(--border);border-radius:7px;font-size:12px;color:var(--text);outline:none;font-family:'Plus Jakarta Sans',sans-serif;cursor:pointer;}
.fsel:focus{border-color:var(--green);}
.gap{flex:1;}
.bulk-bar{display:none;align-items:center;gap:8px;background:var(--charcoal);border-radius:8px;padding:7px 14px;color:#fff;font-size:12.5px;font-weight:600;}
.bulk-bar.show{display:flex;}
.bulk-count{background:var(--green);border-radius:20px;padding:1px 8px;font-size:11px;}

/* ── ROLE BANNER ────────────────────────── */
.rbanner{display:flex;align-items:center;gap:12px;background:var(--charcoal2);border-radius:10px;padding:13px 16px;margin-bottom:18px;color:#fff;border:1px solid rgba(255,255,255,.05);}
.rbanner svg{color:var(--green);flex-shrink:0;}
.rb-title{font-family:'Space Grotesk',sans-serif;font-size:13.5px;font-weight:700;}
.rb-sub{font-size:11px;opacity:.48;margin-top:1px;}

/* ── MODAL ──────────────────────────────── */
.overlay{position:fixed;inset:0;background:rgba(10,14,22,.52);z-index:300;display:none;align-items:center;justify-content:center;backdrop-filter:blur(4px);}
.overlay.open{display:flex;animation:fdin .18s ease;}
@keyframes fdin{from{opacity:0}to{opacity:1}}
.modal{background:var(--surface);border-radius:13px;width:640px;max-width:96vw;max-height:90vh;overflow-y:auto;box-shadow:var(--shadow-lg);animation:sup .22s cubic-bezier(.4,0,.2,1);border:1px solid var(--border);}
.modal.wide{width:800px;}
.modal.narrow{width:480px;}
@keyframes sup{from{transform:translateY(24px);opacity:0}to{transform:translateY(0);opacity:1}}
.mhead{padding:20px 22px 16px;border-bottom:1px solid var(--border);display:flex;align-items:flex-start;justify-content:space-between;gap:14px;}
.mhead-t{font-family:'Space Grotesk',sans-serif;font-size:17px;font-weight:700;letter-spacing:-.02em;}
.mhead-s{font-size:12px;color:var(--text-sub);margin-top:2px;}
.mxbtn{width:28px;height:28px;border-radius:7px;border:1px solid var(--border);background:none;cursor:pointer;display:flex;align-items:center;justify-content:center;color:var(--text-sub);transition:all .16s;flex-shrink:0;}
.mxbtn:hover{background:#fee2e2;color:#dc2626;border-color:#fca5a5;}
.mxbtn svg{width:12px;height:12px;}
.mbody{padding:20px 22px;}
.mfoot{padding:12px 22px;border-top:1px solid var(--border);display:flex;align-items:center;justify-content:flex-end;gap:8px;}
.mfoot-left{margin-right:auto;}

/* ── FORM ───────────────────────────────── */
.fgrid{display:grid;grid-template-columns:1fr 1fr;gap:14px;}
.fg{display:flex;flex-direction:column;gap:5px;}
.fg.full{grid-column:1/-1;}
.fg.third{grid-column:span 1;}
.fg label{font-size:11.5px;font-weight:700;color:var(--text);}
.fg label em{color:#ef4444;font-style:normal;}
.fg input,.fg select,.fg textarea{padding:9px 11px;border:1.5px solid var(--border);border-radius:7px;font-family:'Plus Jakarta Sans',sans-serif;font-size:12.5px;color:var(--text);outline:none;transition:border-color .2s;width:100%;background:#fff;line-height:1.4;}
.fg input:focus,.fg select:focus,.fg textarea:focus{border-color:var(--green);box-shadow:0 0 0 3px rgba(109,190,79,.1);}
.fg textarea{resize:vertical;min-height:76px;}
.uparea{border:2px dashed var(--border);border-radius:9px;padding:22px 14px;text-align:center;cursor:pointer;transition:all .2s;background:#fafbfc;}
.uparea:hover{border-color:var(--green);background:#f4fcf0;}
.up-ico{color:var(--gray4);margin:0 auto 7px;}
.up-txt{font-size:12.5px;color:var(--text-sub);}
.up-txt strong{color:var(--green);}
.up-hint{font-size:10.5px;color:var(--gray4);margin-top:3px;}
.uparea.has-file{border-color:var(--green);background:#f4fcf0;}
.file-chip{display:inline-flex;align-items:center;gap:6px;background:var(--green-lt);border:1px solid var(--green);border-radius:6px;padding:4px 10px;font-size:11.5px;font-weight:600;color:var(--green-dk);margin-top:6px;}

/* ── DETAIL ─────────────────────────────── */
.dsec{margin-bottom:18px;}
.dsec-h{font-size:10px;font-weight:700;letter-spacing:.10em;text-transform:uppercase;color:var(--text-sub);padding-bottom:7px;border-bottom:1px solid var(--border);margin-bottom:11px;}
.dgrid{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.df-l{font-size:10.5px;color:var(--text-sub);margin-bottom:2px;}
.df-v{font-size:12.5px;font-weight:600;}
.comment-c{background:#f8fbff;border-radius:8px;padding:11px 13px;border-left:3px solid var(--blue-dk);margin-bottom:9px;}
.cc-meta{font-size:10.5px;color:var(--text-sub);margin-bottom:3px;display:flex;align-items:center;justify-content:space-between;}
.cc-text{font-size:12.5px;line-height:1.5;}
.notes-c{background:#f8f9fc;border-radius:8px;padding:11px 13px;border-left:3px solid var(--blue);margin-top:10px;}
.nl{font-size:10px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--text-sub);margin-bottom:3px;}
.nt{font-size:12.5px;line-height:1.5;}
.tl{display:flex;flex-direction:column;}
.tl-item{display:flex;gap:10px;}
.tl-spine{display:flex;flex-direction:column;align-items:center;width:18px;flex-shrink:0;}
.tl-dot{width:9px;height:9px;border-radius:50%;border:2px solid var(--green);background:#fff;margin-top:2px;flex-shrink:0;}
.tl-dot.f{background:var(--green);}
.tl-ln{flex:1;width:1.5px;background:var(--border);margin:3px 0;min-height:12px;}
.tl-bd{padding-bottom:13px;}
.tl-t{font-size:12.5px;font-weight:600;}
.tl-s{font-size:11px;color:var(--text-sub);margin-top:1px;}
.rev-actions{display:flex;gap:8px;margin-top:12px;flex-wrap:wrap;}

/* ── KANBAN ─────────────────────────────── */
.kanban-board{display:grid;grid-template-columns:repeat(5,1fr);gap:14px;align-items:start;}
.kb-col{background:#f8f9fb;border-radius:10px;border:1px solid var(--border);overflow:hidden;}
.kb-head{padding:10px 12px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--border);}
.kb-head-title{font-size:11.5px;font-weight:700;display:flex;align-items:center;gap:6px;}
.kb-count{background:var(--border);border-radius:20px;padding:1px 7px;font-size:10px;font-weight:700;color:var(--text-sub);}
.kb-body{padding:8px;display:flex;flex-direction:column;gap:6px;min-height:80px;}
.kb-card{background:#fff;border-radius:8px;border:1px solid var(--border);padding:10px 12px;cursor:pointer;transition:box-shadow .16s,transform .16s;}
.kb-card:hover{box-shadow:var(--shadow-md);transform:translateY(-1px);}
.kb-card-id{font-family:'Space Grotesk',sans-serif;font-size:10.5px;font-weight:700;color:var(--blue-dk);margin-bottom:4px;}
.kb-card-title{font-size:12px;font-weight:600;line-height:1.3;margin-bottom:6px;}
.kb-card-meta{display:flex;align-items:center;justify-content:space-between;margin-top:6px;}
.kb-card-av{width:20px;height:20px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:8px;font-weight:700;color:#fff;flex-shrink:0;}
.kb-empty{font-size:11px;color:var(--gray4);text-align:center;padding:16px 8px;}

/* ── CALENDAR ───────────────────────────── */
.cal-grid{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;}
.cal-header{display:grid;grid-template-columns:repeat(7,1fr);gap:4px;margin-bottom:4px;}
.cal-day-name{text-align:center;font-size:10.5px;font-weight:700;color:var(--text-sub);padding:4px;}
.cal-day{min-height:72px;border-radius:8px;border:1px solid var(--border);background:#fff;padding:6px;transition:background .12s;}
.cal-day:hover{background:#f5fbf2;}
.cal-day.today{border-color:var(--green);background:var(--green-lt);}
.cal-day.other-month{background:#f8f9fb;opacity:.5;}
.cal-day-num{font-size:11.5px;font-weight:700;color:var(--text-sub);margin-bottom:3px;}
.cal-day.today .cal-day-num{color:var(--green-dk);}
.cal-event{font-size:10px;background:var(--blue-dk);color:#fff;border-radius:4px;padding:1px 5px;margin-bottom:2px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;cursor:pointer;}
.cal-event.ev-pending{background:#f59e0b;}
.cal-event.ev-approved{background:#16a34a;}
.cal-event.ev-rejected{background:#dc2626;}
.cal-event.ev-revision{background:#7c3aed;}
.cal-nav{display:flex;align-items:center;justify-content:space-between;margin-bottom:14px;}
.cal-month{font-family:'Space Grotesk',sans-serif;font-size:15px;font-weight:700;}

/* ── NOTIFICATIONS PANEL ────────────────── */
.notif-panel{position:fixed;top:60px;right:20px;width:340px;background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow-lg);z-index:200;display:none;animation:sup .2s ease;overflow:hidden;}
.notif-panel.open{display:block;}
.np-head{padding:14px 16px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;}
.np-title{font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;}
.np-mark{font-size:11.5px;color:var(--green);cursor:pointer;font-weight:600;}
.np-list{max-height:360px;overflow-y:auto;}
.np-item{padding:12px 16px;border-bottom:1px solid #f0f2f5;display:flex;gap:10px;cursor:pointer;transition:background .12s;}
.np-item:hover{background:#f5fbf2;}
.np-item.unread{background:#f8fff8;}
.np-item.unread::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--green);}
.np-item{position:relative;}
.np-ico{width:32px;height:32px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:14px;flex-shrink:0;}
.np-txt{flex:1;min-width:0;}
.np-msg{font-size:12.5px;font-weight:500;line-height:1.4;}
.np-time{font-size:10.5px;color:var(--text-sub);margin-top:2px;}
.np-empty{padding:32px;text-align:center;color:var(--text-sub);font-size:13px;}

/* ── SEARCH RESULTS DROPDOWN ────────────── */
.search-dropdown{position:absolute;top:100%;left:0;right:0;background:#fff;border:1px solid var(--border);border-radius:9px;box-shadow:var(--shadow-md);z-index:100;margin-top:4px;overflow:hidden;display:none;}
.search-dropdown.open{display:block;}
.sd-item{padding:10px 14px;display:flex;align-items:center;gap:10px;cursor:pointer;transition:background .12s;border-bottom:1px solid #f0f2f5;}
.sd-item:last-child{border-bottom:none;}
.sd-item:hover{background:#f5fbf2;}
.sd-id{font-family:'Space Grotesk',sans-serif;font-size:11px;font-weight:700;color:var(--blue-dk);flex-shrink:0;}
.sd-title{font-size:12.5px;font-weight:500;}
.sd-sub{font-size:11px;color:var(--text-sub);}
.search-wrap{position:relative;}

/* ── ACTIVITY FEED ──────────────────────── */
.feed-item{display:flex;gap:10px;padding:10px 0;border-bottom:1px solid #f0f2f5;}
.feed-item:last-child{border-bottom:none;}
.feed-ico{width:30px;height:30px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:13px;flex-shrink:0;}
.feed-body{flex:1;min-width:0;}
.feed-msg{font-size:12.5px;line-height:1.4;}
.feed-msg strong{font-weight:700;}
.feed-time{font-size:11px;color:var(--text-sub);margin-top:2px;}

/* ── DUE DATE ───────────────────────────── */
.due-chip{display:inline-flex;align-items:center;gap:4px;font-size:11px;font-weight:600;padding:2px 8px;border-radius:20px;}
.due-ok{background:#dcfce7;color:#166534;}
.due-warn{background:#fef3c7;color:#92400e;}
.due-over{background:#fee2e2;color:#991b1b;}

/* ── PROGRESS BAR LARGE ─────────────────── */
.prog-lg{height:6px;background:var(--gray1);border-radius:3px;overflow:hidden;margin-top:4px;}
.prog-lg-f{height:100%;border-radius:3px;background:var(--green);transition:width .5s;}

/* ── CHECKLIST ──────────────────────────── */
.checklist{display:flex;flex-direction:column;gap:6px;margin-top:8px;}
.check-item{display:flex;align-items:center;gap:8px;padding:7px 10px;background:#f8f9fb;border-radius:7px;cursor:pointer;transition:background .12s;}
.check-item:hover{background:var(--green-lt);}
.check-item input[type=checkbox]{width:14px;height:14px;accent-color:var(--green);cursor:pointer;flex-shrink:0;}
.check-item label{font-size:12.5px;cursor:pointer;flex:1;}
.check-item.done label{text-decoration:line-through;color:var(--text-sub);}

/* ── TAG CHIPS ──────────────────────────── */
.tag-list{display:flex;flex-wrap:wrap;gap:5px;margin-top:6px;}
.tag{display:inline-flex;align-items:center;gap:4px;padding:2px 8px;border-radius:20px;font-size:10.5px;font-weight:600;background:var(--bg);border:1px solid var(--border);color:var(--text-sub);}
.tag.t-urgent{background:#fee2e2;border-color:#fca5a5;color:#991b1b;}
.tag.t-fab{background:#dcfce7;border-color:#86efac;color:#166534;}
.tag.t-erection{background:#dbeafe;border-color:#93c5fd;color:#1e40af;}
.tag.t-civil{background:#fef9c3;border-color:#fde047;color:#854d0e;}
.tag.t-arch{background:#ede9fe;border-color:#c4b5fd;color:#5b21b6;}
.tag-x{cursor:pointer;opacity:.6;font-size:11px;line-height:1;}
.tag-x:hover{opacity:1;}

/* ── STAR ───────────────────────────────── */
.star{color:var(--gray4);cursor:pointer;font-size:14px;transition:color .12s;}
.star.lit{color:#f59e0b;}

/* ── AVATAR STACK ───────────────────────── */
.av-stack{display:flex;}
.av-stack .chip-av{border:2px solid #fff;margin-left:-6px;}
.av-stack .chip-av:first-child{margin-left:0;}

/* ── QUICK STATS ROW ─────────────────────── */
.qs-row{display:flex;gap:16px;flex-wrap:wrap;margin-bottom:18px;}
.qs-item{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--text-sub);}
.qs-val{font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;color:var(--text);}
.qs-dot{width:8px;height:8px;border-radius:50%;}

/* ── SETTINGS ───────────────────────────── */
.settings-grid{display:grid;grid-template-columns:200px 1fr;gap:20px;}
.settings-nav{display:flex;flex-direction:column;gap:2px;}
.stab{padding:8px 12px;border-radius:8px;font-size:13px;font-weight:500;cursor:pointer;color:var(--text-sub);border:none;background:none;font-family:'Plus Jakarta Sans',sans-serif;text-align:left;transition:all .16s;}
.stab.on{background:var(--green-lt);color:var(--green-dk);font-weight:700;}
.stab:hover:not(.on){background:var(--bg);}
.settings-section{display:none;}
.settings-section.on{display:block;}
.setting-row{display:flex;align-items:center;justify-content:space-between;padding:14px 0;border-bottom:1px solid var(--border);}
.setting-row:last-child{border-bottom:none;}
.sr-label{font-size:13px;font-weight:600;}
.sr-sub{font-size:11.5px;color:var(--text-sub);margin-top:2px;}
.toggle-sw{position:relative;width:36px;height:20px;cursor:pointer;}
.toggle-sw input{opacity:0;width:0;height:0;}
.toggle-track{position:absolute;inset:0;background:var(--gray1);border-radius:20px;transition:.2s;}
.toggle-track::before{content:'';position:absolute;left:2px;top:2px;width:16px;height:16px;border-radius:50%;background:#fff;transition:.2s;}
.toggle-sw input:checked + .toggle-track{background:var(--green);}
.toggle-sw input:checked + .toggle-track::before{transform:translateX(16px);}
.color-picker-row{display:flex;gap:8px;margin-top:8px;}
.color-opt{width:24px;height:24px;border-radius:50%;cursor:pointer;border:2px solid transparent;transition:all .16s;}
.color-opt.selected,.color-opt:hover{border-color:var(--text);transform:scale(1.15);}

/* ── EXPORT ─────────────────────────────── */
.export-options{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.export-card{border:1.5px solid var(--border);border-radius:9px;padding:14px;cursor:pointer;transition:all .16s;text-align:center;}
.export-card:hover{border-color:var(--green);background:var(--green-lt);}
.export-card svg{color:var(--green);margin:0 auto 8px;}
.export-card-title{font-size:13px;font-weight:700;}
.export-card-sub{font-size:11px;color:var(--text-sub);margin-top:2px;}

/* ── TOAST ──────────────────────────────── */
.toast{position:fixed;bottom:20px;right:20px;z-index:9999;background:var(--charcoal2);color:#fff;padding:11px 16px;border-radius:10px;font-size:12.5px;font-weight:500;display:flex;align-items:center;gap:8px;box-shadow:var(--shadow-lg);border-left:3px solid var(--green);transform:translateY(64px);opacity:0;transition:all .28s cubic-bezier(.34,1.56,.64,1);max-width:300px;}
.toast.show{transform:translateY(0);opacity:1;}

/* ── EMPTY ──────────────────────────────── */
.empty{text-align:center;padding:46px 16px;color:var(--text-sub);}
.empty-ico{font-size:38px;margin-bottom:9px;}
.empty-title{font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;color:var(--text);margin-bottom:4px;}
.empty-sub{font-size:12px;}

/* ── VIEWS ──────────────────────────────── */
.view{display:none;} .view.on{display:block;}

/* ── LEGEND ─────────────────────────────── */
.leg-row{display:flex;align-items:center;justify-content:space-between;padding:4px 0;}
.leg-dot{width:8px;height:8px;border-radius:50%;flex-shrink:0;}
.leg-nm{font-size:12px;color:var(--text-sub);}
.leg-vl{font-size:12px;font-weight:700;font-family:'Space Grotesk',sans-serif;}

/* ── DEADLINE BADGE ─────────────────────── */
.dl-badge{display:inline-flex;align-items:center;gap:3px;font-size:10.5px;padding:2px 7px;border-radius:5px;font-weight:600;}

/* RESPONSIVE */
::-webkit-scrollbar{width:4px;height:4px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:var(--gray1);border-radius:4px;}

/* PRINT */
@media print{.sidebar,.topbar,.tb-right,.arow,.frow,.tabs,.bulk-bar{display:none!important;}.main{margin-left:0;}}
</style>
</head>
<body>

<!-- ═══ SIDEBAR ═══════════════════════════════════════════════ -->
<aside class="sidebar">
  <div class="slogo">
    <div class="slogo-inner">
      <div class="slogo-icon">CP</div>
      <div>
        <div class="slogo-name">Check<em>Print</em></div>
        <div class="slogo-tag">DBM Vircon</div>
      </div>
    </div>
  </div>
  <nav class="snav">
    <span class="snav-label">Main</span>
    <a class="snav-item active" onclick="nav('dashboard',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/></svg>
      <span class="lbl">Dashboard</span>
    </a>
    <a class="snav-item" onclick="nav('list',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
      <span class="lbl">Check Prints</span><span class="snav-badge" id="nb-p">3</span>
    </a>
    <a class="snav-item" onclick="nav('kanban',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="3" width="5" height="11" rx="1"/><rect x="10" y="3" width="5" height="7" rx="1"/><rect x="17" y="3" width="5" height="14" rx="1"/></svg>
      <span class="lbl">Kanban Board</span>
    </a>
    <a class="snav-item" onclick="nav('calendar',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
      <span class="lbl">Calendar</span>
    </a>
    <a class="snav-item" onclick="nav('submit',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="16"/><line x1="8" y1="12" x2="16" y2="12"/></svg>
      <span class="lbl">Submit New</span>
    </a>
    <span class="snav-label" style="margin-top:10px;">Review</span>
    <a class="snav-item" onclick="nav('review',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
      <span class="lbl">Review Queue</span><span class="snav-badge" id="nb-q">2</span>
    </a>
    <a class="snav-item" onclick="nav('history',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
      <span class="lbl">History</span>
    </a>
    <span class="snav-label" style="margin-top:10px;">Reports</span>
    <a class="snav-item" onclick="nav('reports',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>
      <span class="lbl">Analytics</span>
    </a>
    <a class="snav-item" onclick="nav('settings',this)">
      <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>
      <span class="lbl">Settings</span>
    </a>
  </nav>
  <div class="suser">
    <div class="suser-av" id="s-av">JD</div>
    <div style="flex:1;min-width:0;">
      <div class="suser-name" id="s-nm">John Doe</div>
      <div class="suser-role" id="s-rl">Detailer</div>
    </div>
  </div>
</aside>

<!-- ═══ MAIN ═══════════════════════════════════════════════════ -->
<main class="main">
  <header class="topbar">
    <div>
      <div class="tb-title" id="pg-t">Dashboard</div>
      <div class="tb-crumb">
        <span>DBM Vircon</span>
        <svg width="9" height="9" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><polyline points="9 18 15 12 9 6"/></svg>
        <span id="pg-c">Overview</span>
      </div>
    </div>
    <div class="search-wrap">
      <div class="searchbox" id="searchbox">
        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <input type="text" placeholder="Search ID, title, detailer…" id="search-in" oninput="doSearch(this.value)" onblur="setTimeout(()=>closeSD(),150)" onfocus="doSearch(this.value)">
      </div>
      <div class="search-dropdown" id="search-dd"></div>
    </div>
    <div class="tb-right">
      <div class="role-sw">
        <button class="role-pill on" id="rp-d" onclick="setRole('detailer')">Detailer</button>
        <button class="role-pill" id="rp-c" onclick="setRole('checker')">Checker</button>
      </div>
      <div class="ico-btn" id="notif-btn" onclick="toggleNotif()">
        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/></svg>
        <div class="notif-dot" id="notif-pip"></div>
      </div>
      <button class="btn btn-outline btn-sm" onclick="openM('m-export')">
        <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" width="13" height="13"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
        Export
      </button>
      <button class="btn btn-primary" onclick="openM('ms')">
        <svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        New Check Print
      </button>
    </div>
  </header>

  <!-- NOTIFICATIONS PANEL -->
  <div class="notif-panel" id="notif-panel">
    <div class="np-head">
      <div class="np-title">Notifications <span id="notif-unread-count" style="font-size:11px;background:var(--green);color:#fff;border-radius:20px;padding:1px 7px;margin-left:4px;font-weight:700;"></span></div>
      <span class="np-mark" onclick="markAllRead()">Mark all read</span>
    </div>
    <div class="np-list" id="np-list"></div>
  </div>

  <div class="content">

    <!-- ══ DASHBOARD ═══════════════════════════════════════════ -->
    <div class="view on" id="view-dashboard">
      <div class="rbanner">
        <svg width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        <div style="flex:1;">
          <div class="rb-title" id="rb-t">Welcome, Detailer — John Doe</div>
          <div class="rb-sub">Check print overview &middot; <span id="today-d"></span></div>
        </div>
        <div style="display:flex;gap:6px;">
          <button class="btn btn-sm" style="background:rgba(255,255,255,.1);color:#fff;border:none;" onclick="openM('m-export')">Export Report</button>
          <button class="btn btn-primary btn-sm" onclick="openM('ms')">+ Submit</button>
        </div>
      </div>

      <!-- KPI CARDS -->
      <div class="stats-row">
        <div class="stat-card sc-g">
          <div class="stat-label">Total Submitted</div><div class="stat-num" id="ss-tot">0</div><div class="stat-hint">This month</div>
          <div class="stat-bgico"><svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg></div>
        </div>
        <div class="stat-card sc-b">
          <div class="stat-label">Pending Review</div><div class="stat-num" id="ss-pnd">0</div><div class="stat-hint">Awaiting checker</div>
          <div class="stat-bgico"><svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg></div>
        </div>
        <div class="stat-card sc-t">
          <div class="stat-label">Approved</div><div class="stat-num" id="ss-app">0</div><div class="stat-hint">All time</div>
          <div class="stat-bgico"><svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg></div>
        </div>
        <div class="stat-card sc-a">
          <div class="stat-label">Revision Needed</div><div class="stat-num" id="ss-rvis">0</div><div class="stat-hint">Needs attention</div>
          <div class="stat-bgico"><svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg></div>
        </div>
        <div class="stat-card sc-gr">
          <div class="stat-label">Rejected</div><div class="stat-num" id="ss-rej">0</div><div class="stat-hint">All time</div>
          <div class="stat-bgico"><svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg></div>
        </div>
      </div>

      <!-- MAIN DASH GRID -->
      <div style="display:grid;grid-template-columns:1fr 1fr 290px;gap:16px;margin-bottom:16px;">
        <!-- Recent -->
        <div style="grid-column:1/3;">
          <div class="sec-hd">
            <div class="sec-title">Recent Check Prints</div>
            <div style="display:flex;gap:6px;">
              <button class="btn btn-outline btn-sm" onclick="nav('list',document.querySelectorAll('.snav-item')[1])">View All</button>
            </div>
          </div>
          <div class="card"><table>
            <thead><tr><th>ID</th><th>Drawing</th><th>Detailer</th><th>Status</th><th>Due</th><th></th></tr></thead>
            <tbody id="dash-tb"></tbody>
          </table></div>
        </div>
        <!-- Donut -->
        <div>
          <div class="sec-hd"><div class="sec-title">Status Breakdown</div></div>
          <div class="card card-pad" style="text-align:center;">
            <canvas id="donut" width="250" height="168" style="display:block;margin:0 auto;"></canvas>
            <div id="dleg" style="margin-top:10px;display:flex;flex-direction:column;gap:1px;"></div>
          </div>
        </div>
      </div>

      <!-- BOTTOM DASH GRID -->
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
        <!-- Activity Feed -->
        <div>
          <div class="sec-hd"><div class="sec-title">Activity Feed</div></div>
          <div class="card card-pad" style="max-height:280px;overflow-y:auto;" id="activity-feed"></div>
        </div>
        <!-- Overdue -->
        <div>
          <div class="sec-hd"><div class="sec-title">Starred &amp; Overdue</div></div>
          <div class="card card-pad" id="starred-list"></div>
        </div>
      </div>
    </div>

    <!-- ══ LIST ════════════════════════════════════════════════ -->
    <div class="view" id="view-list">
      <div class="tabs" id="stabs">
        <button class="tab on" onclick="tabF('all',this)">All</button>
        <button class="tab" onclick="tabF('Pending',this)">Pending</button>
        <button class="tab" onclick="tabF('In Review',this)">In Review</button>
        <button class="tab" onclick="tabF('Approved',this)">Approved</button>
        <button class="tab" onclick="tabF('Revision',this)">Revision</button>
        <button class="tab" onclick="tabF('Rejected',this)">Rejected</button>
      </div>
      <div class="frow">
        <select class="fsel" id="f-proj" onchange="buildList()"><option value="">All Projects</option><option>Structural Steel Package A</option><option>Mechanical Package B</option><option>Architectural Cladding</option><option>Civil Infrastructure</option></select>
        <select class="fsel" id="f-prio" onchange="buildList()"><option value="">All Priorities</option><option>High</option><option>Medium</option><option>Low</option></select>
        <select class="fsel" id="f-checker" onchange="buildList()"><option value="">All Checkers</option><option>Sarah Chen</option><option>Mike Torres</option><option>Priya Nair</option></select>
        <select class="fsel" id="f-tag" onchange="buildList()"><option value="">All Tags</option><option>Urgent</option><option>Fabrication</option><option>Erection</option><option>Civil</option><option>Architectural</option></select>
        <div class="gap"></div>
        <!-- Bulk Bar -->
        <div class="bulk-bar" id="bulk-bar">
          <span class="bulk-count" id="bulk-count">0</span> selected
          <button class="btn btn-sm btn-approve" onclick="bulkAction('Approved')">Approve All</button>
          <button class="btn btn-sm btn-reject" onclick="bulkAction('Rejected')">Reject All</button>
          <button class="btn btn-sm" style="background:rgba(255,255,255,.15);color:#fff;border:none;" onclick="clearSelection()">Clear</button>
        </div>
        <button class="btn btn-outline btn-sm" onclick="openM('m-export')">
          <svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24" width="12" height="12"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
          Export
        </button>
        <button class="btn btn-primary btn-sm" onclick="openM('ms')">
          <svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="12" height="12"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          Submit New
        </button>
      </div>
      <div class="card"><table>
        <thead><tr>
          <th style="width:36px;"><input type="checkbox" id="select-all" onchange="toggleSelectAll(this)" style="accent-color:var(--green);"></th>
          <th onclick="sortBy('id')" title="Sort">ID <span class="sort-ico" id="si-id">↕</span></th>
          <th onclick="sortBy('title')" title="Sort">Drawing <span class="sort-ico" id="si-title">↕</span></th>
          <th>Detailer</th>
          <th onclick="sortBy('proj')" title="Sort">Project <span class="sort-ico" id="si-proj">↕</span></th>
          <th onclick="sortBy('prio')" title="Sort">Priority <span class="sort-ico" id="si-prio">↕</span></th>
          <th onclick="sortBy('status')" title="Sort">Status <span class="sort-ico" id="si-status">↕</span></th>
          <th>Progress</th>
          <th onclick="sortBy('due')" title="Sort">Due <span class="sort-ico" id="si-due">↕</span></th>
          <th>Tags</th>
          <th>Actions</th>
        </tr></thead>
        <tbody id="list-tb"></tbody>
      </table></div>
    </div>

    <!-- ══ KANBAN ═══════════════════════════════════════════════ -->
    <div class="view" id="view-kanban">
      <div class="sec-hd" style="margin-bottom:14px;">
        <div class="sec-title">Kanban Board</div>
        <button class="btn btn-primary btn-sm" onclick="openM('ms')">+ New Check Print</button>
      </div>
      <div class="kanban-board" id="kanban-board"></div>
    </div>

    <!-- ══ CALENDAR ══════════════════════════════════════════════ -->
    <div class="view" id="view-calendar">
      <div class="cal-nav">
        <button class="btn btn-outline btn-sm" onclick="calMove(-1)">&#8592; Prev</button>
        <div class="cal-month" id="cal-month-label"></div>
        <button class="btn btn-outline btn-sm" onclick="calMove(1)">Next &#8594;</button>
      </div>
      <div class="cal-header" id="cal-header"></div>
      <div class="cal-grid" id="cal-grid"></div>
    </div>

    <!-- ══ SUBMIT ════════════════════════════════════════════════ -->
    <div class="view" id="view-submit">
      <div class="card" style="max-width:660px;">
        <div class="card-pad">
          <div style="margin-bottom:18px;">
            <div style="font-family:'Space Grotesk',sans-serif;font-size:17px;font-weight:700;letter-spacing:-.02em;margin-bottom:3px;">Submit Check Print</div>
            <div style="font-size:12px;color:var(--text-sub);">Fill in all details and upload your drawing for review</div>
          </div>
          <div class="fgrid">
            <div class="fg"><label>Drawing Title <em>*</em></label><input id="sf-t" placeholder="Foundation Detail Sheet 01"></div>
            <div class="fg"><label>Drawing Number <em>*</em></label><input id="sf-n" placeholder="ST-001-A"></div>
            <div class="fg"><label>Project <em>*</em></label><select id="sf-pj"><option>Structural Steel Package A</option><option>Mechanical Package B</option><option>Architectural Cladding</option><option>Civil Infrastructure</option></select></div>
            <div class="fg"><label>Priority</label><select id="sf-pr"><option>Medium</option><option>High</option><option>Low</option></select></div>
            <div class="fg"><label>Revision</label><input id="sf-rv" value="Rev A"></div>
            <div class="fg"><label>Assign Checker</label><select id="sf-ck"><option>Sarah Chen</option><option>Mike Torres</option><option>Priya Nair</option></select></div>
            <div class="fg"><label>Due Date</label><input type="date" id="sf-due"></div>
            <div class="fg"><label>Tags</label>
              <select id="sf-tag" onchange="addTagFromSelect(this,'sf-tags-list')"><option value="">Add tag…</option><option value="Urgent">Urgent</option><option value="Fabrication">Fabrication</option><option value="Erection">Erection</option><option value="Civil">Civil</option><option value="Architectural">Architectural</option></select>
              <div class="tag-list" id="sf-tags-list"></div>
            </div>
            <div class="fg full"><label>Notes for Checker</label><textarea id="sf-nt" placeholder="Any special notes…"></textarea></div>
            <div class="fg full"><label>Upload Drawing</label>
              <div class="uparea" id="sf-uparea" onclick="document.getElementById('sf-fi').click()">
                <div class="up-ico"><svg width="30" height="30" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg></div>
                <div class="up-txt"><strong>Click to upload</strong> or drag &amp; drop</div>
                <div class="up-hint" id="sf-fn">PDF, DWG, DXF up to 50 MB</div>
                <input type="file" id="sf-fi" style="display:none" accept=".pdf,.dwg,.dxf,.png,.jpg" onchange="handleFileSelect(this,'sf-fn','sf-uparea')">
              </div>
            </div>
            <!-- Review Checklist -->
            <div class="fg full">
              <label>Self-Review Checklist</label>
              <div class="checklist" id="self-checklist">
                <div class="check-item"><input type="checkbox" id="chk1"><label for="chk1">Title block complete (project name, drawing no., revision, date)</label></div>
                <div class="check-item"><input type="checkbox" id="chk2"><label for="chk2">Scale bar and north point included</label></div>
                <div class="check-item"><input type="checkbox" id="chk3"><label for="chk3">All dimensions checked and consistent</label></div>
                <div class="check-item"><input type="checkbox" id="chk4"><label for="chk4">Weld/bolt specifications match project spec</label></div>
                <div class="check-item"><input type="checkbox" id="chk5"><label for="chk5">Cross-references to other drawings verified</label></div>
              </div>
            </div>
          </div>
          <div style="display:flex;gap:8px;margin-top:18px;">
            <button class="btn btn-primary" onclick="submitPage()">
              <svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><path d="M22 2L11 13"/><path d="M22 2L15 22 11 13 2 9l20-7z"/></svg>
              Submit Check Print
            </button>
            <button class="btn btn-outline" onclick="nav('list',document.querySelectorAll('.snav-item')[1])">Cancel</button>
          </div>
        </div>
      </div>
    </div>

    <!-- ══ REVIEW QUEUE ═══════════════════════════════════════════ -->
    <div class="view" id="view-review">
      <div class="rbanner" style="background:linear-gradient(135deg,#0a3260,#0d4282);">
        <svg width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg>
        <div><div class="rb-title">Checker Review Queue</div><div class="rb-sub">Review and action check prints assigned to you</div></div>
      </div>
      <div class="qs-row" id="qs-review"></div>
      <div class="card"><table>
        <thead><tr><th>ID</th><th>Drawing</th><th>Detailer</th><th>Priority</th><th>Status</th><th>Due</th><th>Tags</th><th>Action</th></tr></thead>
        <tbody id="rev-tb"></tbody>
      </table></div>
    </div>

    <!-- ══ HISTORY ════════════════════════════════════════════════ -->
    <div class="view" id="view-history">
      <div class="sec-hd" style="margin-bottom:12px;">
        <div class="sec-title">Check Print History</div>
        <div style="display:flex;gap:6px;">
          <button class="btn btn-outline btn-sm" onclick="openM('m-export')">Export CSV</button>
        </div>
      </div>
      <div class="card"><table>
        <thead><tr><th>ID</th><th>Drawing</th><th>Detailer</th><th>Checker</th><th>Final Status</th><th>Revision Rounds</th><th>Submitted</th><th>Resolved</th><th></th></tr></thead>
        <tbody id="hist-tb"></tbody>
      </table></div>
    </div>

    <!-- ══ ANALYTICS ═════════════════════════════════════════════ -->
    <div class="view" id="view-reports">
      <div class="stats-row">
        <div class="stat-card sc-g"><div class="stat-label">Approval Rate</div><div class="stat-num" id="r-aprate">0%</div><div class="stat-hint">Last 30 days</div></div>
        <div class="stat-card sc-b"><div class="stat-label">Avg Review Time</div><div class="stat-num">2.4d</div><div class="stat-hint">Per check print</div></div>
        <div class="stat-card sc-t"><div class="stat-label">Revision Rate</div><div class="stat-num" id="r-revrate">0%</div><div class="stat-hint">Requires rework</div></div>
        <div class="stat-card sc-a"><div class="stat-label">Submissions</div><div class="stat-num" id="r-subs">0</div><div class="stat-hint">This month</div></div>
        <div class="stat-card sc-gr"><div class="stat-label">Active Checkers</div><div class="stat-num">3</div><div class="stat-hint">Assigned</div></div>
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px;">
        <div class="card card-pad">
          <div class="sec-title" style="margin-bottom:14px;">Monthly Submissions</div>
          <canvas id="bar-ch" width="380" height="180" style="display:block;width:100%;"></canvas>
        </div>
        <div class="card card-pad">
          <div class="sec-title" style="margin-bottom:14px;">Status Distribution</div>
          <canvas id="donut2" width="380" height="180" style="display:block;width:100%;"></canvas>
        </div>
      </div>
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
        <div class="card card-pad">
          <div class="sec-title" style="margin-bottom:12px;">Detailer Performance</div>
          <div id="perf-table"></div>
        </div>
        <div class="card card-pad">
          <div class="sec-title" style="margin-bottom:12px;">Project Breakdown</div>
          <canvas id="proj-bar" width="380" height="180" style="display:block;width:100%;"></canvas>
        </div>
      </div>
    </div>

    <!-- ══ SETTINGS ══════════════════════════════════════════════ -->
    <div class="view" id="view-settings">
      <div class="sec-hd" style="margin-bottom:16px;"><div class="sec-title">Settings &amp; Preferences</div></div>
      <div class="card card-pad">
        <div class="settings-grid">
          <div class="settings-nav">
            <button class="stab on" onclick="openStab('general',this)">General</button>
            <button class="stab" onclick="openStab('notifications',this)">Notifications</button>
            <button class="stab" onclick="openStab('team',this)">Team</button>
            <button class="stab" onclick="openStab('workflow',this)">Workflow</button>
          </div>
          <div>
            <!-- GENERAL -->
            <div class="settings-section on" id="stab-general">
              <div style="font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;margin-bottom:14px;">General Preferences</div>
              <div class="setting-row">
                <div><div class="sr-label">Default Checker</div><div class="sr-sub">Auto-assign to this checker on submission</div></div>
                <select class="fsel"><option>Sarah Chen</option><option>Mike Torres</option><option>Priya Nair</option></select>
              </div>
              <div class="setting-row">
                <div><div class="sr-label">Dark Mode</div><div class="sr-sub">Switch to dark interface</div></div>
                <label class="toggle-sw"><input type="checkbox" onchange="toggleDark(this)"><div class="toggle-track"></div></label>
              </div>
              <div class="setting-row">
                <div><div class="sr-label">Compact View</div><div class="sr-sub">Reduce table row spacing</div></div>
                <label class="toggle-sw"><input type="checkbox"><div class="toggle-track"></div></label>
              </div>
              <div class="setting-row">
                <div><div class="sr-label">Default Due Days</div><div class="sr-sub">Auto-set due date on submission</div></div>
                <select class="fsel"><option>3 days</option><option>5 days</option><option>7 days</option><option>14 days</option></select>
              </div>
            </div>
            <!-- NOTIFICATIONS -->
            <div class="settings-section" id="stab-notifications">
              <div style="font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;margin-bottom:14px;">Notification Preferences</div>
              <div class="setting-row"><div><div class="sr-label">Status Changes</div><div class="sr-sub">Notify when a check print is approved, rejected, or revised</div></div><label class="toggle-sw"><input type="checkbox" checked><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">New Submissions</div><div class="sr-sub">Notify checker on new check print</div></div><label class="toggle-sw"><input type="checkbox" checked><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">Due Date Reminders</div><div class="sr-sub">Alert 24h before due date</div></div><label class="toggle-sw"><input type="checkbox" checked><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">Comments</div><div class="sr-sub">Notify on checker comments</div></div><label class="toggle-sw"><input type="checkbox"><div class="toggle-track"></div></label></div>
            </div>
            <!-- TEAM -->
            <div class="settings-section" id="stab-team">
              <div style="font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;margin-bottom:14px;">Team Members</div>
              <div id="team-list"></div>
              <button class="btn btn-outline btn-sm" style="margin-top:12px;" onclick="toast('Team invite sent!','📧')">+ Invite Member</button>
            </div>
            <!-- WORKFLOW -->
            <div class="settings-section" id="stab-workflow">
              <div style="font-family:'Space Grotesk',sans-serif;font-size:14px;font-weight:700;margin-bottom:14px;">Workflow Rules</div>
              <div class="setting-row"><div><div class="sr-label">Require Self-Review Checklist</div><div class="sr-sub">Detailer must complete checklist before submitting</div></div><label class="toggle-sw"><input type="checkbox" checked><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">Auto-escalate High Priority</div><div class="sr-sub">Escalate to senior checker if overdue by 2 days</div></div><label class="toggle-sw"><input type="checkbox"><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">Require Comment on Rejection</div><div class="sr-sub">Checker must provide reason when rejecting</div></div><label class="toggle-sw"><input type="checkbox" checked><div class="toggle-track"></div></label></div>
              <div class="setting-row"><div><div class="sr-label">Max Revision Rounds</div><div class="sr-sub">Auto-flag if exceeded</div></div>
                <select class="fsel"><option>2</option><option>3</option><option>5</option><option>Unlimited</option></select>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div><!-- /content -->
</main>

<!-- ═══ MODALS ════════════════════════════════════════════════ -->

<!-- SUBMIT MODAL -->
<div class="overlay" id="ms" onclick="if(event.target===this)closeM('ms')">
  <div class="modal">
    <div class="mhead">
      <div><div class="mhead-t">Submit Check Print</div><div class="mhead-s">Upload your drawing for checker review</div></div>
      <button class="mxbtn" onclick="closeM('ms')"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>
    </div>
    <div class="mbody">
      <div class="fgrid">
        <div class="fg"><label>Drawing Title <em>*</em></label><input id="mf-t" placeholder="Foundation Detail Sheet 01"></div>
        <div class="fg"><label>Drawing Number <em>*</em></label><input id="mf-n" placeholder="ST-001-A"></div>
        <div class="fg"><label>Project <em>*</em></label><select id="mf-pj"><option>Structural Steel Package A</option><option>Mechanical Package B</option><option>Architectural Cladding</option><option>Civil Infrastructure</option></select></div>
        <div class="fg"><label>Priority</label><select id="mf-pr"><option>Medium</option><option>High</option><option>Low</option></select></div>
        <div class="fg"><label>Revision</label><input id="mf-rv" value="Rev A"></div>
        <div class="fg"><label>Assign Checker</label><select id="mf-ck"><option>Sarah Chen</option><option>Mike Torres</option><option>Priya Nair</option></select></div>
        <div class="fg"><label>Due Date</label><input type="date" id="mf-due"></div>
        <div class="fg"><label>Tags</label>
          <select id="mf-tag" onchange="addTagFromSelect(this,'mf-tags-list')"><option value="">Add tag…</option><option value="Urgent">Urgent</option><option value="Fabrication">Fabrication</option><option value="Erection">Erection</option><option value="Civil">Civil</option><option value="Architectural">Architectural</option></select>
          <div class="tag-list" id="mf-tags-list"></div>
        </div>
        <div class="fg full"><label>Notes</label><textarea id="mf-nt" placeholder="Notes for the checker…"></textarea></div>
        <div class="fg full"><label>Upload Drawing</label>
          <div class="uparea" id="mf-uparea" onclick="document.getElementById('mf-fi').click()">
            <div class="up-ico"><svg width="26" height="26" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg></div>
            <div class="up-txt"><strong>Click to upload</strong> or drag &amp; drop</div>
            <div class="up-hint" id="mf-fn">PDF, DWG, DXF up to 50 MB</div>
            <input type="file" id="mf-fi" style="display:none" accept=".pdf,.dwg,.dxf,.png,.jpg" onchange="handleFileSelect(this,'mf-fn','mf-uparea')">
          </div>
        </div>
      </div>
    </div>
    <div class="mfoot">
      <button class="btn btn-outline" onclick="closeM('ms')">Cancel</button>
      <button class="btn btn-primary" onclick="doSubmitModal()">
        <svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><path d="M22 2L11 13"/><path d="M22 2L15 22 11 13 2 9l20-7z"/></svg>
        Submit
      </button>
    </div>
  </div>
</div>

<!-- DETAIL MODAL -->
<div class="overlay" id="md" onclick="if(event.target===this)closeM('md')">
  <div class="modal wide">
    <div class="mhead">
      <div><div class="mhead-t" id="dm-t">Detail</div><div class="mhead-s" id="dm-s">—</div></div>
      <div style="display:flex;align-items:center;gap:6px;">
        <button class="btn btn-outline btn-sm" id="dm-star-btn" onclick="toggleStar(currentDetailId)">☆ Star</button>
        <button class="mxbtn" onclick="closeM('md')"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>
      </div>
    </div>
    <div class="mbody" id="dm-b"></div>
    <div class="mfoot">
      <div class="mfoot-left">
        <button class="btn btn-ghost btn-sm" id="dm-print-btn" onclick="printDetail()">🖨 Print</button>
      </div>
      <button class="btn btn-outline" onclick="closeM('md')">Close</button>
    </div>
  </div>
</div>

<!-- EXPORT MODAL -->
<div class="overlay" id="m-export" onclick="if(event.target===this)closeM('m-export')">
  <div class="modal narrow">
    <div class="mhead">
      <div><div class="mhead-t">Export Data</div><div class="mhead-s">Choose a format to export check prints</div></div>
      <button class="mxbtn" onclick="closeM('m-export')"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>
    </div>
    <div class="mbody">
      <div class="export-options">
        <div class="export-card" onclick="doExport('csv')">
          <svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="display:block;margin:0 auto 8px;"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>
          <div class="export-card-title">CSV Spreadsheet</div>
          <div class="export-card-sub">All check prints as .csv</div>
        </div>
        <div class="export-card" onclick="doExport('json')">
          <svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="display:block;margin:0 auto 8px;"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
          <div class="export-card-title">JSON Data</div>
          <div class="export-card-sub">Full data as .json</div>
        </div>
        <div class="export-card" onclick="doExport('print')">
          <svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="display:block;margin:0 auto 8px;"><polyline points="6 9 6 2 18 2 18 9"/><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2"/><rect x="6" y="14" width="12" height="8"/></svg>
          <div class="export-card-title">Print Report</div>
          <div class="export-card-sub">Print-friendly view</div>
        </div>
        <div class="export-card" onclick="doExport('summary')">
          <svg width="28" height="28" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" style="display:block;margin:0 auto 8px;"><line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><line x1="3" y1="6" x2="3.01" y2="6"/><line x1="3" y1="12" x2="3.01" y2="12"/><line x1="3" y1="18" x2="3.01" y2="18"/></svg>
          <div class="export-card-title">Summary Text</div>
          <div class="export-card-sub">Plain text summary</div>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="tk"><span id="tk-i" style="font-size:14px;flex-shrink:0;">✅</span><span id="tk-m">Done</span></div>

<script>
// ══════════════════════════════════════════════════════════════
// DATA
// ══════════════════════════════════════════════════════════════
const TAGS_MAP={Urgent:'t-urgent',Fabrication:'t-fab',Erection:'t-erection',Civil:'t-civil',Architectural:'t-arch'};
const DCOLS={'John Doe':'#0077C8','Amir Mehta':'#6DBE4F','Riya Kumar':'#3F873F','Ben Liu':'#636569','Sarah Chen':'#7c3aed','Mike Torres':'#0077C8','Priya Nair':'#3F873F'};
const ini=n=>n.split(' ').map(w=>w[0]).join('').toUpperCase();
const today=()=>new Date().toISOString().split('T')[0];
const nowStr=()=>new Date().toLocaleString('sv').replace('T',' ');

const DB=[
  {id:'CP-001',title:'Foundation Detail Sheet 01',num:'ST-001-A',proj:'Structural Steel Package A',prio:'High',status:'Pending',detailer:'John Doe',checker:'Sarah Chen',rev:'Rev A',date:'2025-03-10',due:'2026-04-05',notes:'Please review connection details on Section B-B.',tags:['Urgent','Fabrication'],starred:true,comments:[],log:[{a:'Submitted',by:'John Doe',d:'2025-03-10 09:22'}]},
  {id:'CP-002',title:'Column Splice Connection',num:'ST-002-B',proj:'Structural Steel Package A',prio:'High',status:'In Review',detailer:'Amir Mehta',checker:'Mike Torres',rev:'Rev A',date:'2025-03-08',due:'2026-04-03',notes:'Check bolt spacing per spec 8.4.2.',tags:['Erection'],starred:false,comments:[{by:'Mike Torres',d:'2025-03-09 14:10',txt:'Checking weld call-outs now.'}],log:[{a:'Submitted',by:'Amir Mehta',d:'2025-03-08 11:00'},{a:'Review Started',by:'Mike Torres',d:'2025-03-09 09:00'}]},
  {id:'CP-003',title:'Roof Purlin Layout Plan',num:'ST-003-A',proj:'Structural Steel Package A',prio:'Medium',status:'Approved',detailer:'John Doe',checker:'Sarah Chen',rev:'Rev B',date:'2025-02-28',due:'2026-03-20',notes:'',tags:['Fabrication'],starred:false,comments:[{by:'Sarah Chen',d:'2025-03-01 10:00',txt:'All dimensions verified. Approved.'}],log:[{a:'Submitted',by:'John Doe',d:'2025-02-28 08:30'},{a:'Approved',by:'Sarah Chen',d:'2025-03-01 10:05'}]},
  {id:'CP-004',title:'Mechanical Duct Routing Level 2',num:'MC-011-A',proj:'Mechanical Package B',prio:'Medium',status:'Revision',detailer:'Riya Kumar',checker:'Priya Nair',rev:'Rev A',date:'2025-03-05',due:'2026-04-08',notes:'Coordinate with services drawing.',tags:['Civil'],starred:false,comments:[{by:'Priya Nair',d:'2025-03-07 15:30',txt:'Duct sizes on Grid D-E not matching spec. Please revise.'}],log:[{a:'Submitted',by:'Riya Kumar',d:'2025-03-05 13:00'},{a:'Revision Requested',by:'Priya Nair',d:'2025-03-07 15:32'}]},
  {id:'CP-005',title:'External Cladding Profile Detail',num:'AR-007-A',proj:'Architectural Cladding',prio:'Low',status:'Pending',detailer:'Ben Liu',checker:'Sarah Chen',rev:'Rev A',date:'2025-03-12',due:'2026-04-15',notes:'',tags:['Architectural'],starred:false,comments:[],log:[{a:'Submitted',by:'Ben Liu',d:'2025-03-12 10:00'}]},
  {id:'CP-006',title:'Staircase Handrail Detail',num:'ST-009-A',proj:'Structural Steel Package A',prio:'Medium',status:'Approved',detailer:'Amir Mehta',checker:'Mike Torres',rev:'Rev C',date:'2025-03-01',due:'2026-03-25',notes:'',tags:['Erection'],starred:false,comments:[{by:'Mike Torres',d:'2025-03-03 09:00',txt:'Verified against BS EN spec. Approved.'}],log:[{a:'Submitted',by:'Amir Mehta',d:'2025-03-01 09:00'},{a:'Revision Requested',by:'Mike Torres',d:'2025-03-02 11:00'},{a:'Resubmitted',by:'Amir Mehta',d:'2025-03-02 16:00'},{a:'Approved',by:'Mike Torres',d:'2025-03-03 09:05'}]},
  {id:'CP-007',title:'Civil Drainage Culvert Layout',num:'CV-004-B',proj:'Civil Infrastructure',prio:'High',status:'Rejected',detailer:'Riya Kumar',checker:'Priya Nair',rev:'Rev A',date:'2025-02-20',due:'2026-03-15',notes:'',tags:['Civil'],starred:false,comments:[{by:'Priya Nair',d:'2025-02-22 16:00',txt:'Missing title block, north point and scale bar. Please redo.'}],log:[{a:'Submitted',by:'Riya Kumar',d:'2025-02-20 14:00'},{a:'Rejected',by:'Priya Nair',d:'2025-02-22 16:05'}]},
  {id:'CP-008',title:'Anchor Bolt Setting Out Plan',num:'ST-012-A',proj:'Structural Steel Package A',prio:'Medium',status:'Pending',detailer:'John Doe',checker:'Sarah Chen',rev:'Rev A',date:'2025-03-14',due:'2026-04-10',notes:'Check with structural engineer mark-up.',tags:['Fabrication','Urgent'],starred:true,comments:[],log:[{a:'Submitted',by:'John Doe',d:'2025-03-14 08:45'}]},
];

let NOTIFS=[
  {id:1,msg:'<strong>CP-002</strong> was assigned for your review',time:'2 hours ago',ico:'📋',read:false,cpId:'CP-002'},
  {id:2,msg:'<strong>CP-001</strong> is due in 5 days',time:'3 hours ago',ico:'⏰',read:false,cpId:'CP-001'},
  {id:3,msg:'<strong>CP-003</strong> was approved by Sarah Chen',time:'Yesterday',ico:'✅',read:true,cpId:'CP-003'},
  {id:4,msg:'<strong>CP-007</strong> was rejected — please revise',time:'2 days ago',ico:'❌',read:true,cpId:'CP-007'},
];

let ACTIVITY=[
  {msg:'<strong>Sarah Chen</strong> approved <strong>CP-003</strong>',time:'2 hours ago',ico:'✅'},
  {msg:'<strong>Priya Nair</strong> requested revision on <strong>CP-004</strong>',time:'3 hours ago',ico:'🔄'},
  {msg:'<strong>John Doe</strong> submitted <strong>CP-008</strong>',time:'Yesterday',ico:'📤'},
  {msg:'<strong>Mike Torres</strong> started review of <strong>CP-002</strong>',time:'Yesterday',ico:'🔍'},
  {msg:'<strong>Riya Kumar</strong> submitted <strong>CP-005</strong>',time:'2 days ago',ico:'📤'},
];

const TEAM=[
  {name:'John Doe',role:'Detailer',av:'JD',col:'#0077C8',status:'Active'},
  {name:'Amir Mehta',role:'Detailer',av:'AM',col:'#6DBE4F',status:'Active'},
  {name:'Riya Kumar',role:'Detailer',av:'RK',col:'#3F873F',status:'Active'},
  {name:'Ben Liu',role:'Detailer',av:'BL',col:'#636569',status:'Active'},
  {name:'Sarah Chen',role:'Checker',av:'SC',col:'#7c3aed',status:'Active'},
  {name:'Mike Torres',role:'Checker',av:'MT',col:'#0077C8',status:'Active'},
  {name:'Priya Nair',role:'Checker',av:'PN',col:'#3F873F',status:'Active'},
];

let role='detailer', stab='all', sortCol='id', sortAsc=true;
let selectedIds=new Set();
let currentDetailId=null;
let calYear=2026, calMonth=3; // April 2026

// ══════════════════════════════════════════════════════════════
// HELPERS
// ══════════════════════════════════════════════════════════════
function mkBadge(s){const m={Pending:'b-pen','In Review':'b-rev',Approved:'b-app',Rejected:'b-rej',Revision:'b-rvis'};return`<span class="badge ${m[s]||'b-pen'}"><span class="bd"></span>${s}</span>`;}
function mkPrio(p){const c=p==='High'?'ph':p==='Low'?'pl':'pm';return`<span class="prio-cell"><span class="pdot ${c}"></span>${p}</span>`;}
function mkProg(s){const pct={Pending:10,'In Review':45,Approved:100,Rejected:100,Revision:60}[s]||10;const col=s==='Approved'?'#22c55e':s==='Rejected'?'#ef4444':s==='Revision'?'#8b5cf6':'#0077C8';return`<div class="prog"><div class="prog-f" style="width:${pct}%;background:${col};"></div></div>`;}
function mkChip(name,sm){const c=DCOLS[name]||'#636569';const s=sm?20:24;return`<div class="chip"><div class="chip-av" style="background:${c};width:${s}px;height:${s}px;font-size:${sm?8:10}px;">${ini(name)}</div><span class="chip-n" style="font-size:${sm?11:12}px;">${name}</span></div>`;}
function mkDue(due){if(!due)return'—';const d=new Date(due),now2=new Date(),diff=Math.round((d-now2)/(1000*60*60*24));if(diff<0)return`<span class="due-chip due-over">⚠ Overdue ${Math.abs(diff)}d</span>`;if(diff<=3)return`<span class="due-chip due-warn">⏰ ${diff}d left</span>`;return`<span class="due-chip due-ok">📅 ${due}</span>`;}
function mkTags(tags){if(!tags||!tags.length)return'';return tags.map(t=>`<span class="tag ${TAGS_MAP[t]||''}">${t}</span>`).join('');}
function mkStar(starred,id){return`<span class="star ${starred?'lit':''}" onclick="event.stopPropagation();toggleStar('${id}')" title="${starred?'Unstar':'Star'}">${starred?'★':'☆'}</span>`;}

// ══════════════════════════════════════════════════════════════
// ROLE
// ══════════════════════════════════════════════════════════════
function setRole(r){
  role=r;
  document.getElementById('rp-d').classList.toggle('on',r==='detailer');
  document.getElementById('rp-c').classList.toggle('on',r==='checker');
  const nm=r==='detailer'?'John Doe':'Sarah Chen';
  document.getElementById('s-nm').textContent=nm;
  document.getElementById('s-rl').textContent=r==='detailer'?'Detailer':'Checker';
  document.getElementById('s-av').textContent=r==='detailer'?'JD':'SC';
  document.getElementById('rb-t').textContent=`Welcome, ${r==='detailer'?'Detailer':'Checker'} — ${nm}`;
  refresh();
  toast(`Switched to ${r==='detailer'?'Detailer':'Checker'} view`,r==='checker'?'🔍':'✏️');
}

// ══════════════════════════════════════════════════════════════
// NAV
// ══════════════════════════════════════════════════════════════
const PNM={dashboard:'Dashboard',list:'Check Prints',kanban:'Kanban Board',calendar:'Calendar',submit:'Submit New',review:'Review Queue',history:'History',reports:'Analytics',settings:'Settings'};
function nav(id,el){
  document.querySelectorAll('.view').forEach(v=>v.classList.remove('on'));
  document.getElementById('view-'+id)?.classList.add('on');
  document.querySelectorAll('.snav-item').forEach(n=>n.classList.remove('active'));
  if(el)el.classList.add('active');
  document.getElementById('pg-t').textContent=PNM[id]||id;
  document.getElementById('pg-c').textContent=PNM[id]||id;
  if(id==='dashboard'){refreshDash();setTimeout(drawDonut,60);}
  if(id==='list')buildList();
  if(id==='review')buildRev();
  if(id==='history')buildHist();
  if(id==='kanban')buildKanban();
  if(id==='calendar')buildCalendar();
  if(id==='reports')setTimeout(()=>{drawBar();drawDonut2();drawProjBar();buildPerfTable();},80);
  if(id==='settings')buildTeamList();
  closeNotifPanel();
}

// ══════════════════════════════════════════════════════════════
// REFRESH
// ══════════════════════════════════════════════════════════════
function refresh(){
  refreshDash();buildList();buildRev();buildHist();drawDonut();
  document.getElementById('nb-p').textContent=DB.filter(c=>c.status==='Pending').length;
  document.getElementById('nb-q').textContent=DB.filter(c=>c.status==='Pending'||c.status==='In Review').length;
  refreshNotifBadge();
}

function refreshDash(){
  document.getElementById('ss-tot').textContent=DB.length;
  document.getElementById('ss-pnd').textContent=DB.filter(c=>c.status==='Pending').length;
  document.getElementById('ss-app').textContent=DB.filter(c=>c.status==='Approved').length;
  document.getElementById('ss-rvis').textContent=DB.filter(c=>c.status==='Revision').length;
  document.getElementById('ss-rej').textContent=DB.filter(c=>c.status==='Rejected').length;
  // Recent table
  const tb=document.getElementById('dash-tb');
  tb.innerHTML=[...DB].slice(-6).reverse().map(cp=>`
    <tr onclick="openDetail('${cp.id}')">
      <td><span class="cp-id">${cp.id}</span></td>
      <td><div style="display:flex;align-items:center;gap:6px;">${mkStar(cp.starred,cp.id)}<div><div class="cp-name" style="max-width:200px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${cp.title}</div><div class="cp-meta">${cp.num} &middot; ${cp.rev}</div></div></div></td>
      <td>${mkChip(cp.detailer,true)}</td>
      <td>${mkBadge(cp.status)}</td>
      <td>${mkDue(cp.due)}</td>
      <td onclick="event.stopPropagation()"><div class="arow">
        <button class="abtn" onclick="openDetail('${cp.id}')" title="View"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg></button>
      </div></td>
    </tr>`).join('');
  // Activity
  const af=document.getElementById('activity-feed');
  if(af)af.innerHTML=ACTIVITY.map(a=>`
    <div class="feed-item">
      <div class="feed-ico" style="background:var(--bg);">${a.ico}</div>
      <div class="feed-body"><div class="feed-msg">${a.msg}</div><div class="feed-time">${a.time}</div></div>
    </div>`).join('');
  // Starred / Overdue
  const sl=document.getElementById('starred-list');
  if(sl){
    const starred=DB.filter(c=>c.starred);
    const overdue=DB.filter(c=>{if(!c.due)return false;return new Date(c.due)<new Date()&&c.status!=='Approved'&&c.status!=='Rejected';});
    const combined=[...new Map([...starred,...overdue].map(x=>[x.id,x])).values()];
    sl.innerHTML=combined.length?combined.map(cp=>`
      <div style="display:flex;align-items:center;gap:8px;padding:8px 0;border-bottom:1px solid #f0f2f5;cursor:pointer;" onclick="openDetail('${cp.id}')">
        <span class="star lit" onclick="event.stopPropagation();toggleStar('${cp.id}')">★</span>
        <div style="flex:1;min-width:0;">
          <div style="font-size:12.5px;font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${cp.title}</div>
          <div style="font-size:11px;color:var(--text-sub);">${cp.id} &middot; ${mkDue(cp.due)}</div>
        </div>
        ${mkBadge(cp.status)}
      </div>`).join(''):`<div class="empty" style="padding:24px;"><div class="empty-ico">⭐</div><div class="empty-sub">No starred or overdue items</div></div>`;
  }
}

// ══════════════════════════════════════════════════════════════
// LIST TABLE
// ══════════════════════════════════════════════════════════════
function buildList(){
  let data=[...DB];
  if(stab!=='all')data=data.filter(c=>c.status===stab);
  const pj=document.getElementById('f-proj')?.value;
  const pr=document.getElementById('f-prio')?.value;
  const ck=document.getElementById('f-checker')?.value;
  const tg=document.getElementById('f-tag')?.value;
  if(pj)data=data.filter(c=>c.proj===pj);
  if(pr)data=data.filter(c=>c.prio===pr);
  if(ck)data=data.filter(c=>c.checker===ck);
  if(tg)data=data.filter(c=>c.tags&&c.tags.includes(tg));
  // Sort
  data.sort((a,b)=>{
    let av=a[sortCol]||'',bv=b[sortCol]||'';
    const priMap={High:3,Medium:2,Low:1};
    if(sortCol==='prio'){av=priMap[av]||0;bv=priMap[bv]||0;}
    if(av<bv)return sortAsc?-1:1;
    if(av>bv)return sortAsc?1:-1;
    return 0;
  });
  const tb=document.getElementById('list-tb');
  if(!data.length){tb.innerHTML=`<tr><td colspan="11"><div class="empty"><div class="empty-ico">🔍</div><div class="empty-title">No check prints found</div><div class="empty-sub">Adjust your filters</div></div></td></tr>`;return;}
  tb.innerHTML=data.map(cp=>`
    <tr onclick="openDetail('${cp.id}')" class="${selectedIds.has(cp.id)?'selected-row':''}">
      <td onclick="event.stopPropagation()"><input type="checkbox" style="accent-color:var(--green);" ${selectedIds.has(cp.id)?'checked':''} onchange="toggleSelect('${cp.id}',this)"></td>
      <td><span class="cp-id">${cp.id}</span></td>
      <td><div style="display:flex;align-items:center;gap:5px;">${mkStar(cp.starred,cp.id)}<div><div class="cp-name" style="max-width:180px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${cp.title}</div><div class="cp-meta">${cp.num} &middot; ${cp.rev}</div></div></div></td>
      <td>${mkChip(cp.detailer,true)}</td>
      <td style="font-size:11.5px;color:var(--text-sub);max-width:110px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">${cp.proj}</td>
      <td>${mkPrio(cp.prio)}</td>
      <td>${mkBadge(cp.status)}</td>
      <td>${mkProg(cp.status)}</td>
      <td>${mkDue(cp.due)}</td>
      <td><div class="tag-list" style="flex-wrap:nowrap;overflow:hidden;max-width:100px;">${mkTags(cp.tags)}</div></td>
      <td onclick="event.stopPropagation()"><div class="arow">
        <button class="abtn" onclick="openDetail('${cp.id}')" title="View"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg></button>
        ${role==='checker'&&(cp.status==='Pending'||cp.status==='In Review')?`
        <button class="abtn aok" onclick="quickA('${cp.id}','Approved')" title="Approve"><svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg></button>
        <button class="abtn ano" onclick="quickA('${cp.id}','Rejected')" title="Reject"><svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></button>`:''}
      </div></td>
    </tr>`).join('');
}

// ── SORT ──────────────────────────────────────────────────────
function sortBy(col){
  if(sortCol===col)sortAsc=!sortAsc; else{sortCol=col;sortAsc=true;}
  document.querySelectorAll('.sort-ico').forEach(el=>el.textContent='↕');
  const si=document.getElementById('si-'+col);
  if(si){si.textContent=sortAsc?'↑':'↓';si.parentElement.classList.add('sorted');}
  buildList();
}

// ── SELECTION ─────────────────────────────────────────────────
function toggleSelect(id,cb){
  if(cb.checked)selectedIds.add(id); else selectedIds.delete(id);
  updateBulkBar();buildList();
}
function toggleSelectAll(cb){
  document.querySelectorAll('#list-tb input[type=checkbox]').forEach(c=>{
    const row=c.closest('tr');const id=row?.querySelector('.cp-id')?.textContent;
    if(id){if(cb.checked)selectedIds.add(id);else selectedIds.delete(id);}
  });
  updateBulkBar();buildList();
}
function updateBulkBar(){
  const bb=document.getElementById('bulk-bar');
  const cnt=document.getElementById('bulk-count');
  if(selectedIds.size>0){bb.classList.add('show');cnt.textContent=selectedIds.size;}
  else bb.classList.remove('show');
}
function clearSelection(){selectedIds.clear();document.getElementById('select-all').checked=false;updateBulkBar();buildList();}
function bulkAction(status){
  selectedIds.forEach(id=>{
    const cp=DB.find(c=>c.id===id);
    if(cp&&(cp.status==='Pending'||cp.status==='In Review')){
      cp.status=status;
      cp.log.push({a:status,by:'Sarah Chen',d:nowStr()});
      addActivity(`<strong>Sarah Chen</strong> bulk-${status.toLowerCase()}d <strong>${id}</strong>`,'today','📋');
    }
  });
  toast(`${selectedIds.size} items marked ${status}`,status==='Approved'?'✅':'❌');
  clearSelection();refresh();
}

// ══════════════════════════════════════════════════════════════
// REVIEW TABLE
// ══════════════════════════════════════════════════════════════
function buildRev(){
  const q=DB.filter(c=>c.status==='Pending'||c.status==='In Review');
  const tb=document.getElementById('rev-tb');
  // quick stats
  const qs=document.getElementById('qs-review');
  if(qs)qs.innerHTML=`
    <div class="qs-item"><div class="qs-dot" style="background:#f59e0b;"></div><span>Pending:</span><span class="qs-val">${DB.filter(c=>c.status==='Pending').length}</span></div>
    <div class="qs-item"><div class="qs-dot" style="background:#3b82f6;"></div><span>In Review:</span><span class="qs-val">${DB.filter(c=>c.status==='In Review').length}</span></div>
    <div class="qs-item"><div class="qs-dot" style="background:#ef4444;"></div><span>High Priority:</span><span class="qs-val">${q.filter(c=>c.prio==='High').length}</span></div>
    <div class="qs-item"><div class="qs-dot" style="background:var(--green);"></div><span>Due Today:</span><span class="qs-val">${q.filter(c=>c.due===today()).length}</span></div>`;
  if(!q.length){tb.innerHTML=`<tr><td colspan="8"><div class="empty"><div class="empty-ico">🎉</div><div class="empty-title">All caught up!</div><div class="empty-sub">No items in queue</div></div></td></tr>`;return;}
  tb.innerHTML=q.map(cp=>`
    <tr onclick="openDetail('${cp.id}')">
      <td><span class="cp-id">${cp.id}</span></td>
      <td><div class="cp-name">${cp.title}</div><div class="cp-meta">${cp.num}</div></td>
      <td>${mkChip(cp.detailer,true)}</td>
      <td>${mkPrio(cp.prio)}</td>
      <td>${mkBadge(cp.status)}</td>
      <td>${mkDue(cp.due)}</td>
      <td>${mkTags(cp.tags)}</td>
      <td onclick="event.stopPropagation()"><button class="btn btn-primary btn-sm" onclick="openDetail('${cp.id}')">Review</button></td>
    </tr>`).join('');
}

// ══════════════════════════════════════════════════════════════
// HISTORY
// ══════════════════════════════════════════════════════════════
function buildHist(){
  const done=DB.filter(c=>c.status==='Approved'||c.status==='Rejected');
  const tb=document.getElementById('hist-tb');
  if(!done.length){tb.innerHTML=`<tr><td colspan="9"><div class="empty"><div class="empty-ico">📂</div><div class="empty-title">No history yet</div></div></td></tr>`;return;}
  tb.innerHTML=done.map(cp=>{
    const revRounds=cp.log.filter(l=>l.a==='Revision Requested').length;
    return`<tr onclick="openDetail('${cp.id}')">
      <td><span class="cp-id">${cp.id}</span></td>
      <td><div class="cp-name">${cp.title}</div></td>
      <td style="font-size:12px;">${cp.detailer}</td>
      <td style="font-size:12px;color:var(--text-sub);">${cp.checker}</td>
      <td>${mkBadge(cp.status)}</td>
      <td style="text-align:center;"><span style="font-family:'Space Grotesk',sans-serif;font-weight:700;font-size:13px;color:${revRounds>1?'#dc2626':revRounds===1?'#f59e0b':'#16a34a'};">${revRounds}</span></td>
      <td style="color:var(--text-sub);font-size:11.5px;">${cp.date}</td>
      <td style="color:var(--text-sub);font-size:11.5px;">${cp.log.at(-1)?.d?.split(' ')[0]||'—'}</td>
      <td onclick="event.stopPropagation()"><button class="abtn" onclick="openDetail('${cp.id}')"><svg fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg></button></td>
    </tr>`;}).join('');
}

// ══════════════════════════════════════════════════════════════
// KANBAN
// ══════════════════════════════════════════════════════════════
function buildKanban(){
  const cols=['Pending','In Review','Revision','Approved','Rejected'];
  const colColors={'Pending':'#f59e0b','In Review':'#0077C8','Revision':'#7c3aed','Approved':'#16a34a','Rejected':'#dc2626'};
  const board=document.getElementById('kanban-board');
  board.innerHTML=cols.map(col=>{
    const cards=DB.filter(c=>c.status===col);
    return`<div class="kb-col">
      <div class="kb-head">
        <div class="kb-head-title"><span style="width:8px;height:8px;border-radius:50%;background:${colColors[col]};display:inline-block;"></span>${col}</div>
        <span class="kb-count">${cards.length}</span>
      </div>
      <div class="kb-body">
        ${cards.length?cards.map(cp=>`
          <div class="kb-card" onclick="openDetail('${cp.id}')">
            <div class="kb-card-id">${cp.id} &middot; ${cp.rev}</div>
            <div class="kb-card-title">${cp.title}</div>
            <div style="font-size:11px;color:var(--text-sub);margin-bottom:4px;">${cp.proj.split(' ').slice(0,3).join(' ')}</div>
            ${mkTags(cp.tags)}
            <div class="kb-card-meta">
              ${mkChip(cp.detailer,true)}
              <div style="display:flex;align-items:center;gap:4px;">${mkPrio(cp.prio)}${mkDue(cp.due)}</div>
            </div>
          </div>`).join(''):`<div class="kb-empty">No items</div>`}
      </div>
    </div>`;
  }).join('');
}

// ══════════════════════════════════════════════════════════════
// CALENDAR
// ══════════════════════════════════════════════════════════════
function buildCalendar(){
  const dayNames=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
  document.getElementById('cal-header').innerHTML=dayNames.map(d=>`<div class="cal-day-name">${d}</div>`).join('');
  const lbl=new Date(calYear,calMonth,1).toLocaleString('default',{month:'long',year:'numeric'});
  document.getElementById('cal-month-label').textContent=lbl;
  const firstDay=new Date(calYear,calMonth,1).getDay();
  const daysInMonth=new Date(calYear,calMonth+1,0).getDate();
  const prevDays=new Date(calYear,calMonth,0).getDate();
  const todayStr=today();
  let cells='';
  // prev month days
  for(let i=firstDay-1;i>=0;i--){
    cells+=`<div class="cal-day other-month"><div class="cal-day-num">${prevDays-i}</div></div>`;
  }
  // current month
  for(let d=1;d<=daysInMonth;d++){
    const dateStr=`${calYear}-${String(calMonth+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
    const isToday=dateStr===todayStr;
    const events=DB.filter(cp=>cp.due===dateStr);
    cells+=`<div class="cal-day${isToday?' today':''}">
      <div class="cal-day-num">${d}</div>
      ${events.map(e=>`<div class="cal-event ev-${e.status.toLowerCase().replace(' ','-')}" onclick="openDetail('${e.id}')" title="${e.title}">${e.id}</div>`).join('')}
    </div>`;
  }
  // fill remaining
  const total=firstDay+daysInMonth;const rem=(7-total%7)%7;
  for(let d=1;d<=rem;d++)cells+=`<div class="cal-day other-month"><div class="cal-day-num">${d}</div></div>`;
  document.getElementById('cal-grid').innerHTML=cells;
}
function calMove(dir){calMonth+=dir;if(calMonth>11){calMonth=0;calYear++;}if(calMonth<0){calMonth=11;calYear--;}buildCalendar();}

// ══════════════════════════════════════════════════════════════
// DETAIL MODAL
// ══════════════════════════════════════════════════════════════
function openDetail(id){
  const cp=DB.find(c=>c.id===id);if(!cp)return;
  currentDetailId=id;
  document.getElementById('dm-t').textContent=cp.title;
  document.getElementById('dm-s').textContent=`${cp.id} \u00b7 ${cp.num} \u00b7 ${cp.rev}`;
  document.getElementById('dm-star-btn').textContent=cp.starred?'★ Unstar':'☆ Star';
  const canAct=role==='checker'&&(cp.status==='Pending'||cp.status==='In Review');
  // Progress bar
  const pct={Pending:10,'In Review':45,Approved:100,Rejected:100,Revision:60}[cp.status]||10;
  const pCol=cp.status==='Approved'?'#22c55e':cp.status==='Rejected'?'#ef4444':cp.status==='Revision'?'#8b5cf6':'#0077C8';
  document.getElementById('dm-b').innerHTML=`
    <div style="display:flex;align-items:center;gap:12px;margin-bottom:18px;padding:12px 16px;background:var(--bg);border-radius:9px;">
      <div style="flex:1;">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:4px;">
          <span style="font-size:11.5px;font-weight:700;color:var(--text-sub);">REVIEW PROGRESS</span>
          <span style="font-size:12px;font-weight:700;">${pct}%</span>
        </div>
        <div class="prog-lg"><div class="prog-lg-f" style="width:${pct}%;background:${pCol};"></div></div>
      </div>
      <div>${mkBadge(cp.status)}</div>
      <div>${mkDue(cp.due)}</div>
    </div>
    <div class="dsec">
      <div class="dsec-h">Drawing Information</div>
      <div class="dgrid">
        <div><div class="df-l">Status</div><div class="df-v">${mkBadge(cp.status)}</div></div>
        <div><div class="df-l">Priority</div><div class="df-v">${mkPrio(cp.prio)}</div></div>
        <div><div class="df-l">Drawing Number</div><div class="df-v">${cp.num}</div></div>
        <div><div class="df-l">Revision</div><div class="df-v">${cp.rev}</div></div>
        <div><div class="df-l">Project</div><div class="df-v" style="font-size:12px;">${cp.proj}</div></div>
        <div><div class="df-l">Submitted</div><div class="df-v">${cp.date}</div></div>
        <div><div class="df-l">Detailer</div><div class="df-v">${mkChip(cp.detailer)}</div></div>
        <div><div class="df-l">Assigned Checker</div><div class="df-v" style="font-size:12.5px;">${cp.checker}</div></div>
        <div><div class="df-l">Due Date</div><div class="df-v">${mkDue(cp.due)}</div></div>
        <div><div class="df-l">Tags</div><div class="df-v"><div class="tag-list">${mkTags(cp.tags)}</div></div></div>
      </div>
      ${cp.notes?`<div class="notes-c"><div class="nl">Detailer Notes</div><div class="nt">${cp.notes}</div></div>`:''}
    </div>
    ${cp.comments.length?`<div class="dsec"><div class="dsec-h">Checker Comments (${cp.comments.length})</div>${cp.comments.map(c=>`
      <div class="comment-c">
        <div class="cc-meta"><span>💬 ${c.by}</span><span style="color:var(--gray4);">${c.d}</span></div>
        <div class="cc-text">${c.txt}</div>
      </div>`).join('')}</div>`:''}
    ${canAct?`<div class="dsec"><div class="dsec-h">Review Action</div>
      <div class="fg"><label>Checker Comment</label><textarea id="ck-cm" placeholder="Add your review comment…" style="min-height:68px;border:1.5px solid var(--border);border-radius:7px;padding:9px 11px;font-family:'Plus Jakarta Sans',sans-serif;font-size:12.5px;width:100%;outline:none;"></textarea></div>
      <div class="rev-actions" style="margin-top:10px;">
        <button class="btn btn-approve" onclick="doAction('${cp.id}','Approved')"><svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><polyline points="20 6 9 17 4 12"/></svg>Approve</button>
        <button class="btn btn-revise" onclick="doAction('${cp.id}','Revision')"><svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>Request Revision</button>
        <button class="btn btn-reject" onclick="doAction('${cp.id}','Rejected')"><svg fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" width="13" height="13"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>Reject</button>
      </div>
    </div>`:''}
    <div class="dsec"><div class="dsec-h">Activity Timeline (${cp.log.length} events)</div>
      <div class="tl">${cp.log.map((h,i)=>`
        <div class="tl-item">
          <div class="tl-spine"><div class="tl-dot${i===cp.log.length-1?' f':''}"></div>${i<cp.log.length-1?'<div class="tl-ln"></div>':''}</div>
          <div class="tl-bd"><div class="tl-t">${h.a} <span style="font-weight:400;color:var(--text-sub)">by ${h.by}</span></div><div class="tl-s">${h.d}</div></div>
        </div>`).join('')}
      </div>
    </div>`;
  openM('md');
}

function toggleStar(id){
  const cp=DB.find(c=>c.id===id);if(!cp)return;
  cp.starred=!cp.starred;
  refresh();
  if(currentDetailId===id)document.getElementById('dm-star-btn').textContent=cp.starred?'★ Unstar':'☆ Star';
  toast(cp.starred?`${id} starred`:`${id} unstarred`,cp.starred?'★':'☆');
}

function printDetail(){window.print();}

// ══════════════════════════════════════════════════════════════
// CHECKER ACTIONS
// ══════════════════════════════════════════════════════════════
function doAction(id,ns){
  const cp=DB.find(c=>c.id===id);if(!cp)return;
  const cm=document.getElementById('ck-cm')?.value.trim();
  if(ns==='Rejected'&&!cm){toast('Please add a comment when rejecting','⚠️');return;}
  if(cm)cp.comments.push({by:'Sarah Chen',d:nowStr(),txt:cm});
  cp.status=ns;
  const al={Approved:'Approved',Rejected:'Rejected',Revision:'Revision Requested'};
  cp.log.push({a:al[ns]||ns,by:'Sarah Chen',d:nowStr()});
  const ic={Approved:'✅',Rejected:'❌',Revision:'🔄'};
  const ms={Approved:`${id} approved!`,Rejected:`${id} rejected.`,Revision:`Revision requested for ${id}.`};
  addActivity(`<strong>Sarah Chen</strong> ${al[ns].toLowerCase()} <strong>${id}</strong>`,'just now',ic[ns]);
  addNotif(`<strong>${id}</strong> was ${al[ns].toLowerCase()}`,ic[ns],id);
  closeM('md');refresh();toast(ms[ns]||'Updated',ic[ns]||'ℹ️');
}

function quickA(id,s){
  const cp=DB.find(c=>c.id===id);if(!cp)return;
  cp.status=s;cp.log.push({a:s,by:'Sarah Chen',d:nowStr()});
  addActivity(`<strong>Sarah Chen</strong> ${s.toLowerCase()} <strong>${id}</strong>`,'just now',s==='Approved'?'✅':'❌');
  refresh();toast(`${id} marked ${s}`,s==='Approved'?'✅':'❌');
}

// ══════════════════════════════════════════════════════════════
// SUBMIT
// ══════════════════════════════════════════════════════════════
function doSubmitModal(){
  const t=document.getElementById('mf-t').value.trim();
  const n=document.getElementById('mf-n').value.trim();
  if(!t||!n){toast('Please fill in required fields','⚠️');return;}
  const tags=Array.from(document.getElementById('mf-tags-list').querySelectorAll('.tag')).map(el=>el.dataset.tag);
  addCP({title:t,num:n,proj:document.getElementById('mf-pj').value,prio:document.getElementById('mf-pr').value,rev:document.getElementById('mf-rv').value,checker:document.getElementById('mf-ck').value,notes:document.getElementById('mf-nt').value,due:document.getElementById('mf-due').value,tags});
  document.getElementById('mf-t').value='';document.getElementById('mf-n').value='';document.getElementById('mf-nt').value='';document.getElementById('mf-tags-list').innerHTML='';
  closeM('ms');
}

function submitPage(){
  const t=document.getElementById('sf-t').value.trim();
  const n=document.getElementById('sf-n').value.trim();
  if(!t||!n){toast('Please fill in required fields','⚠️');return;}
  const tags=Array.from(document.getElementById('sf-tags-list').querySelectorAll('.tag')).map(el=>el.dataset.tag);
  addCP({title:t,num:n,proj:document.getElementById('sf-pj').value,prio:document.getElementById('sf-pr').value,rev:document.getElementById('sf-rv').value,checker:document.getElementById('sf-ck').value,notes:document.getElementById('sf-nt').value,due:document.getElementById('sf-due').value,tags});
  nav('list',document.querySelectorAll('.snav-item')[1]);
}

function addCP(d){
  const id=`CP-${String(DB.length+1).padStart(3,'0')}`;
  const nm=role==='detailer'?'John Doe':'Sarah Chen';
  DB.push({id,...d,status:'Pending',detailer:nm,date:today(),starred:false,comments:[],log:[{a:'Submitted',by:nm,d:nowStr()}]});
  addActivity(`<strong>${nm}</strong> submitted <strong>${id}</strong>`,'just now','📤');
  addNotif(`New check print <strong>${id}</strong> submitted by ${nm}`,'📋',id);
  refresh();toast(`${id} submitted!`,'📤');
}

// ── TAGS ──────────────────────────────────────────────────────
function addTagFromSelect(sel,listId){
  const v=sel.value;if(!v)return;
  const list=document.getElementById(listId);
  if(list.querySelector(`[data-tag="${v}"]`)){sel.value='';return;}
  const el=document.createElement('span');
  el.className=`tag ${TAGS_MAP[v]||''}`;el.dataset.tag=v;
  el.innerHTML=`${v}<span class="tag-x" onclick="this.parentElement.remove()">×</span>`;
  list.appendChild(el);sel.value='';
}

// ── FILE ──────────────────────────────────────────────────────
function handleFileSelect(inp,fnId,areaId){
  const f=inp.files[0];if(!f)return;
  document.getElementById(fnId).innerHTML=`<div class="file-chip">📎 ${f.name}</div>`;
  document.getElementById(areaId).classList.add('has-file');
}

// ══════════════════════════════════════════════════════════════
// SEARCH
// ══════════════════════════════════════════════════════════════
function doSearch(q){
  const dd=document.getElementById('search-dd');
  if(!q.trim()){dd.classList.remove('open');return;}
  const lq=q.toLowerCase();
  const res=DB.filter(c=>c.id.toLowerCase().includes(lq)||c.title.toLowerCase().includes(lq)||c.detailer.toLowerCase().includes(lq)||c.num.toLowerCase().includes(lq)).slice(0,6);
  if(!res.length){dd.innerHTML=`<div class="sd-item"><div style="color:var(--text-sub);font-size:12.5px;">No results found</div></div>`;dd.classList.add('open');return;}
  dd.innerHTML=res.map(cp=>`
    <div class="sd-item" onclick="closeSD();openDetail('${cp.id}')">
      <span class="sd-id">${cp.id}</span>
      <div><div class="sd-title">${cp.title}</div><div class="sd-sub">${cp.num} &middot; ${cp.detailer} &middot; ${mkBadge(cp.status)}</div></div>
    </div>`).join('');
  dd.classList.add('open');
}
function closeSD(){document.getElementById('search-dd').classList.remove('open');}

// ══════════════════════════════════════════════════════════════
// NOTIFICATIONS
// ══════════════════════════════════════════════════════════════
function addNotif(msg,ico,cpId){
  NOTIFS.unshift({id:Date.now(),msg,ico,time:'just now',read:false,cpId});
  refreshNotifBadge();renderNotifList();
}
function refreshNotifBadge(){
  const u=NOTIFS.filter(n=>!n.read).length;
  const pip=document.getElementById('notif-pip');
  const cnt=document.getElementById('notif-unread-count');
  if(pip)pip.style.display=u?'block':'none';
  if(cnt)cnt.textContent=u?u:'';
}
function renderNotifList(){
  const list=document.getElementById('np-list');
  if(!list)return;
  if(!NOTIFS.length){list.innerHTML=`<div class="np-empty">No notifications</div>`;return;}
  list.innerHTML=NOTIFS.map(n=>`
    <div class="np-item ${n.read?'':'unread'}" onclick="notifClick('${n.id}','${n.cpId||''}')">
      <div class="np-ico" style="background:${n.read?'var(--bg)':'var(--green-lt)'};">${n.ico}</div>
      <div class="np-txt"><div class="np-msg">${n.msg}</div><div class="np-time">${n.time}</div></div>
    </div>`).join('');
}
function notifClick(nid,cpId){
  const n=NOTIFS.find(x=>x.id==nid);if(n)n.read=true;
  refreshNotifBadge();renderNotifList();
  if(cpId)openDetail(cpId);
}
function markAllRead(){NOTIFS.forEach(n=>n.read=true);refreshNotifBadge();renderNotifList();}
function toggleNotif(){
  const p=document.getElementById('notif-panel');
  p.classList.toggle('open');
  if(p.classList.contains('open'))renderNotifList();
}
function closeNotifPanel(){document.getElementById('notif-panel')?.classList.remove('open');}

// ══════════════════════════════════════════════════════════════
// ACTIVITY FEED
// ══════════════════════════════════════════════════════════════
function addActivity(msg,time,ico){
  ACTIVITY.unshift({msg,time,ico});
  if(ACTIVITY.length>20)ACTIVITY.pop();
}

// ══════════════════════════════════════════════════════════════
// SETTINGS
// ══════════════════════════════════════════════════════════════
function openStab(id,btn){
  document.querySelectorAll('.settings-section').forEach(s=>s.classList.remove('on'));
  document.querySelectorAll('.stab').forEach(b=>b.classList.remove('on'));
  document.getElementById('stab-'+id)?.classList.add('on');
  btn.classList.add('on');
}
function buildTeamList(){
  const el=document.getElementById('team-list');if(!el)return;
  el.innerHTML=TEAM.map(m=>`
    <div style="display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid var(--border);">
      <div class="chip-av" style="background:${m.col};width:32px;height:32px;font-size:11px;">${m.av}</div>
      <div style="flex:1;"><div style="font-size:13px;font-weight:600;">${m.name}</div><div style="font-size:11px;color:var(--text-sub);">${m.role}</div></div>
      <span class="badge b-app" style="font-size:10px;">${m.status}</span>
    </div>`).join('');
}
function toggleDark(cb){
  if(cb.checked){document.documentElement.style.filter='invert(1) hue-rotate(180deg)';}
  else{document.documentElement.style.filter='';}
}

// ══════════════════════════════════════════════════════════════
// EXPORT
// ══════════════════════════════════════════════════════════════
function doExport(type){
  closeM('m-export');
  if(type==='csv'){
    const headers=['ID','Title','Drawing No.','Project','Priority','Status','Detailer','Checker','Revision','Date','Due','Tags'];
    const rows=DB.map(cp=>[cp.id,`"${cp.title}"`,cp.num,`"${cp.proj}"`,cp.prio,cp.status,cp.detailer,cp.checker,cp.rev,cp.date,cp.due||'',(cp.tags||[]).join('|')]);
    const csv=[headers,...rows].map(r=>r.join(',')).join('\n');
    const a=document.createElement('a');a.href='data:text/csv;charset=utf-8,'+encodeURIComponent(csv);a.download='checkprints.csv';a.click();
    toast('CSV exported!','📄');
  }else if(type==='json'){
    const json=JSON.stringify(DB,null,2);
    const a=document.createElement('a');a.href='data:application/json;charset=utf-8,'+encodeURIComponent(json);a.download='checkprints.json';a.click();
    toast('JSON exported!','📦');
  }else if(type==='print'){
    window.print();
  }else if(type==='summary'){
    const lines=['DBM Vircon — Check Print Summary','='.repeat(40),''];
    DB.forEach(cp=>{lines.push(`${cp.id} | ${cp.title} | ${cp.status} | ${cp.detailer} | Due: ${cp.due||'N/A'}`);});
    const a=document.createElement('a');a.href='data:text/plain;charset=utf-8,'+encodeURIComponent(lines.join('\n'));a.download='checkprints-summary.txt';a.click();
    toast('Summary exported!','📝');
  }
}

// ══════════════════════════════════════════════════════════════
// MODALS
// ══════════════════════════════════════════════════════════════
function openM(id){document.getElementById(id).classList.add('open');}
function closeM(id){document.getElementById(id).classList.remove('open');}

// ══════════════════════════════════════════════════════════════
// TOAST
// ══════════════════════════════════════════════════════════════
function toast(msg,ico='✅'){
  const el=document.getElementById('tk');
  document.getElementById('tk-i').textContent=ico;
  document.getElementById('tk-m').textContent=msg;
  el.classList.add('show');clearTimeout(el._t);
  el._t=setTimeout(()=>el.classList.remove('show'),2800);
}

// ══════════════════════════════════════════════════════════════
// ANALYTICS
// ══════════════════════════════════════════════════════════════
function buildPerfTable(){
  const el=document.getElementById('perf-table');if(!el)return;
  const detailers=[...new Set(DB.map(c=>c.detailer))];
  el.innerHTML=`<table style="width:100%">
    <thead><tr><th>Detailer</th><th style="text-align:center;">Submitted</th><th style="text-align:center;">Approved</th><th style="text-align:center;">Revisions</th><th style="text-align:right;">Approval%</th></tr></thead>
    <tbody>${detailers.map(d=>{
      const sub=DB.filter(c=>c.detailer===d);
      const app=sub.filter(c=>c.status==='Approved');
      const rev=sub.reduce((s,c)=>s+c.log.filter(l=>l.a==='Revision Requested').length,0);
      const pct=sub.length?Math.round(app.length/sub.length*100):0;
      return`<tr>
        <td>${mkChip(d,true)}</td>
        <td style="text-align:center;font-family:'Space Grotesk';font-weight:700;">${sub.length}</td>
        <td style="text-align:center;color:#16a34a;font-weight:700;">${app.length}</td>
        <td style="text-align:center;color:#f59e0b;font-weight:700;">${rev}</td>
        <td style="text-align:right;">
          <div style="display:flex;align-items:center;gap:6px;justify-content:flex-end;">
            <div style="width:60px;height:5px;background:var(--gray1);border-radius:3px;overflow:hidden;"><div style="width:${pct}%;height:100%;background:var(--green);border-radius:3px;"></div></div>
            <span style="font-size:12px;font-weight:700;font-family:'Space Grotesk';">${pct}%</span>
          </div>
        </td>
      </tr>`;}).join('')}
    </tbody>
  </table>`;
  // Analytics stats
  const tot=DB.length, app=DB.filter(c=>c.status==='Approved').length, rev=DB.filter(c=>c.status==='Revision').length;
  const arEl=document.getElementById('r-aprate');const rrEl=document.getElementById('r-revrate');const sbEl=document.getElementById('r-subs');
  if(arEl)arEl.textContent=tot?Math.round(app/tot*100)+'%':'0%';
  if(rrEl)rrEl.textContent=tot?Math.round(rev/tot*100)+'%':'0%';
  if(sbEl)sbEl.textContent=DB.length;
}

// ── DONUT ─────────────────────────────────────────────────────
function drawDonut(){drawDonutOn('donut',250,168);}
function drawDonut2(){drawDonutOn('donut2',380,168);}
function drawDonutOn(id,W,H){
  const c=document.getElementById(id);if(!c)return;
  const ctx=c.getContext('2d');c.width=W;c.height=H;
  const sl=[
    {l:'Pending',v:DB.filter(x=>x.status==='Pending').length,c:'#f59e0b'},
    {l:'In Review',v:DB.filter(x=>x.status==='In Review').length,c:'#0077C8'},
    {l:'Approved',v:DB.filter(x=>x.status==='Approved').length,c:'#6DBE4F'},
    {l:'Revision',v:DB.filter(x=>x.status==='Revision').length,c:'#8b5cf6'},
    {l:'Rejected',v:DB.filter(x=>x.status==='Rejected').length,c:'#ef4444'},
  ].filter(d=>d.v>0);
  const tot=sl.reduce((s,d)=>s+d.v,0);if(!tot)return;
  const R=Math.min(W,H)/2-8,ri=R*0.55;
  const cx=R+8,cy=H/2;
  ctx.clearRect(0,0,W,H);
  let a=-Math.PI/2;
  sl.forEach(d=>{
    const sw=d.v/tot*2*Math.PI;
    ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,R,a,a+sw);ctx.closePath();
    ctx.fillStyle=d.c;ctx.fill();a+=sw;
  });
  ctx.beginPath();ctx.arc(cx,cy,ri,0,2*Math.PI);ctx.fillStyle='#fff';ctx.fill();
  ctx.font='bold 18px Arial,sans-serif';ctx.fillStyle='#1a1d23';ctx.textAlign='center';ctx.textBaseline='middle';
  ctx.fillText(tot,cx,cy-6);
  ctx.font='10px Arial,sans-serif';ctx.fillStyle='#636569';ctx.fillText('Total',cx,cy+8);
  // legend on right
  const lx=cx+R+14,ly=cy-sl.length*14;
  sl.forEach((d,i)=>{
    const y=ly+i*22;
    ctx.fillStyle=d.c;ctx.beginPath();ctx.arc(lx+5,y,5,0,2*Math.PI);ctx.fill();
    ctx.font='11px Arial,sans-serif';ctx.fillStyle='#636569';ctx.textAlign='left';ctx.textBaseline='middle';
    ctx.fillText(d.l,lx+13,y);
    ctx.font='bold 11px Arial,sans-serif';ctx.fillStyle='#1a1d23';ctx.textAlign='right';
    ctx.fillText(d.v,W-4,y);
  });
  // Update legend div if exists
  const lg=document.getElementById('dleg');
  if(lg)lg.innerHTML=sl.map(d=>`<div class="leg-row"><div style="display:flex;align-items:center;gap:7px;"><div class="leg-dot" style="background:${d.c};"></div><span class="leg-nm">${d.l}</span></div><span class="leg-vl">${d.v}</span></div>`).join('');
}

// ── BAR CHART ─────────────────────────────────────────────────
function drawBar(){
  const c=document.getElementById('bar-ch');if(!c)return;
  const ctx=c.getContext('2d');
  const W=c.offsetWidth||380;c.width=W;c.height=180;
  const months=['Oct','Nov','Dec','Jan','Feb','Mar'];
  const vals=[4,6,3,8,5,DB.length];
  const max=Math.max(...vals);
  const pL=36,pR=12,pT=18,pB=30;
  const slot=(W-pL-pR)/months.length;const bw=Math.min(38,slot*.55);
  ctx.clearRect(0,0,W,180);
  for(let i=0;i<=4;i++){
    const y=pT+(180-pT-pB)*(1-i/4);
    ctx.strokeStyle='#e3e6ea';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(pL,y);ctx.lineTo(W-pR,y);ctx.stroke();
    ctx.font='10px Arial,sans-serif';ctx.fillStyle='#636569';ctx.textAlign='right';ctx.textBaseline='middle';
    ctx.fillText(Math.round(max*i/4),pL-4,y);
  }
  vals.forEach((v,i)=>{
    const x=pL+i*slot+(slot-bw)/2;const bh=v/max*(180-pT-pB-6);const y=180-pB-bh;
    const gr=ctx.createLinearGradient(0,y,0,180-pB);gr.addColorStop(0,'#6DBE4F');gr.addColorStop(1,'#3F873F');
    ctx.fillStyle=gr;const r=3;
    ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+bw-r,y);ctx.quadraticCurveTo(x+bw,y,x+bw,y+r);ctx.lineTo(x+bw,180-pB);ctx.lineTo(x,180-pB);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath();ctx.fill();
    ctx.font='bold 10px Arial,sans-serif';ctx.fillStyle='#1a1d23';ctx.textAlign='center';ctx.textBaseline='bottom';ctx.fillText(v,x+bw/2,y-2);
    ctx.font='10px Arial,sans-serif';ctx.fillStyle='#636569';ctx.textBaseline='top';ctx.fillText(months[i],x+bw/2,180-pB+5);
  });
}

function drawProjBar(){
  const c=document.getElementById('proj-bar');if(!c)return;
  const ctx=c.getContext('2d');
  const W=c.offsetWidth||380;c.width=W;c.height=180;
  const projs=[...new Set(DB.map(c=>c.proj))];
  const vals=projs.map(p=>DB.filter(c=>c.proj===p).length);
  const max=Math.max(...vals);
  const pL=16,pR=16,pT=16,pB=44;
  const slot=(W-pL-pR)/projs.length;const bw=Math.min(40,slot*.6);
  ctx.clearRect(0,0,W,180);
  vals.forEach((v,i)=>{
    const x=pL+i*slot+(slot-bw)/2;const bh=v/max*(180-pT-pB-6);const y=180-pB-bh;
    const cols=['#0077C8','#6DBE4F','#7c3aed','#f59e0b'];
    ctx.fillStyle=cols[i%cols.length];const r=3;
    ctx.beginPath();ctx.moveTo(x+r,y);ctx.lineTo(x+bw-r,y);ctx.quadraticCurveTo(x+bw,y,x+bw,y+r);ctx.lineTo(x+bw,180-pB);ctx.lineTo(x,180-pB);ctx.lineTo(x,y+r);ctx.quadraticCurveTo(x,y,x+r,y);ctx.closePath();ctx.fill();
    ctx.font='bold 10px Arial,sans-serif';ctx.fillStyle='#1a1d23';ctx.textAlign='center';ctx.textBaseline='bottom';ctx.fillText(v,x+bw/2,y-2);
    // wrap project name
    const words=projs[i].split(' ');let line='';let ly=180-pB+8;
    ctx.font='9px Arial,sans-serif';ctx.fillStyle='#636569';ctx.textBaseline='top';
    words.forEach(w=>{const test=line?line+' '+w:w;if(ctx.measureText(test).width<slot-4){line=test;}else{ctx.fillText(line,x+bw/2,ly);line=w;ly+=11;}});
    if(line)ctx.fillText(line,x+bw/2,ly);
  });
}

// ══════════════════════════════════════════════════════════════
// TAB FILTER & SELECT
// ══════════════════════════════════════════════════════════════
function tabF(s,btn){
  stab=s;
  document.querySelectorAll('#stabs .tab').forEach(b=>b.classList.remove('on'));
  btn.classList.add('on');buildList();
}

// ══════════════════════════════════════════════════════════════
// INIT
// ══════════════════════════════════════════════════════════════
document.getElementById('today-d').textContent=new Date().toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'});
// Close notif on outside click
document.addEventListener('click',e=>{
  if(!document.getElementById('notif-panel')?.contains(e.target)&&!document.getElementById('notif-btn')?.contains(e.target))
    closeNotifPanel();
});
// Tab on checklist
document.querySelectorAll('.check-item').forEach(item=>{
  item.querySelector('input')?.addEventListener('change',function(){item.classList.toggle('done',this.checked);});
});
refresh();
nav('dashboard',document.querySelector('.snav-item'));
</script>
</body>
</html>
